"""
zones.py — Cloud Architecture skill pack zones for NEXUS.
"""

from engine.zone import Zone
from engine.challenge import Challenge

ZONE_ORDER = [
    "cloud_architecture_patterns",
    "multi_cloud_and_hybrid",
    "cost_optimization",
    "high_availability",
    "well_architected_framework",
]

ZONES = {
    "cloud_architecture_patterns": Zone(
        id="cloud_architecture_patterns",
        title="Cloud Architecture Patterns",
        description="Master the foundational design patterns that define modern cloud systems.",
        challenges=[
            Challenge(
                id="ca_microservices_decomposition",
                type="quiz",
                prompt="CIPHER intercepts a monolith migration plan. The PRIMARY reason to decompose into microservices is:",
                options=[
                    "Microservices are always faster than monoliths",
                    "Independent deployment, scaling, and failure isolation per service",
                    "Microservices eliminate the need for databases",
                    "They remove the need for API versioning",
                ],
                answer="b",
                explanation="Microservices let each service scale, deploy, and fail independently. A payment service can scale during peak load while the notification service stays idle. The tradeoff: distributed complexity, network latency, and eventual consistency challenges.",
                xp=10,
            ),
            Challenge(
                id="ca_event_driven_arch",
                type="quiz",
                prompt="In an event-driven architecture, services communicate by:",
                options=[
                    "Direct synchronous HTTP calls between every service",
                    "Publishing events to a broker; consumers subscribe and react asynchronously",
                    "Sharing a single database table for coordination",
                    "Polling each other's health endpoints continuously",
                ],
                answer="b",
                explanation="Event-driven architectures decouple producers from consumers through a message broker (Kafka, SNS/SQS, EventBridge). Services emit events without knowing who consumes them — enabling loose coupling, replay, and audit trails.",
                xp=10,
            ),
            Challenge(
                id="ca_serverless_when",
                type="quiz",
                prompt="CIPHER scans a workload profile. Serverless (FaaS) is the BEST fit for:",
                options=[
                    "A 24/7 high-throughput WebSocket game server",
                    "Sporadic event-triggered processing with variable load and sub-second startup needs",
                    "A monolithic ERP application with 32 GB of in-memory state",
                    "A GPU-intensive ML training pipeline running for 6 hours",
                ],
                answer="b",
                explanation="Serverless excels at bursty, event-driven workloads: pay per invocation, automatic scaling to zero, no infrastructure management. Long-running, stateful, or GPU workloads are better served by containers or dedicated compute.",
                xp=10,
            ),
            Challenge(
                id="ca_cqrs_pattern",
                type="text",
                prompt="CIPHER needs a briefing. Explain CQRS (Command Query Responsibility Segregation): what problem does it solve, and what is the core idea?",
                answer="CQRS separates read and write models",
                explanation="CQRS splits the data model into a write side (commands that mutate state) and a read side (queries optimized for display). This allows independent scaling — heavy read traffic gets denormalized views while writes maintain strict consistency. Often paired with Event Sourcing, where state changes are stored as an immutable event log.",
                xp=15,
            ),
            Challenge(
                id="ca_saga_pattern",
                type="quiz",
                prompt="In a distributed system, the Saga pattern is used to:",
                options=[
                    "Replace all databases with a single global transaction manager",
                    "Manage distributed transactions through a sequence of local transactions with compensating actions on failure",
                    "Cache API responses at the edge for faster reads",
                    "Automatically retry failed HTTP requests indefinitely",
                ],
                answer="b",
                explanation="Sagas break a distributed transaction into local steps, each with a compensating action. If step 3 fails, steps 2 and 1 are rolled back via compensation. Two flavors: choreography (events) and orchestration (central coordinator).",
                xp=15,
            ),
            Challenge(
                id="ca_strangler_fig",
                type="quiz",
                prompt="CIPHER detects a legacy migration in progress. The Strangler Fig pattern works by:",
                options=[
                    "Rewriting the entire monolith from scratch in a single release",
                    "Incrementally replacing monolith functionality by routing new requests to new services while the old system gradually shrinks",
                    "Running two identical copies of the monolith in parallel",
                    "Converting the monolith to serverless functions overnight",
                ],
                answer="b",
                explanation="Named after strangler fig trees that grow around a host tree, this pattern routes traffic for specific features to new microservices via a facade/proxy. Over time, the monolith shrinks to nothing. Zero big-bang risk.",
                xp=10,
            ),
            Challenge(
                id="ca_api_gateway",
                type="quiz",
                prompt="An API Gateway in a microservices architecture provides:",
                options=[
                    "A single entry point handling routing, auth, rate limiting, and request aggregation",
                    "A direct database connection pool shared across all services",
                    "Automatic code deployment to all microservices simultaneously",
                    "A replacement for service-to-service communication",
                ],
                answer="a",
                explanation="The API Gateway is the front door. It handles cross-cutting concerns — authentication, TLS termination, rate limiting, request routing, response caching — so individual services don't have to. Examples: Kong, AWS API Gateway, Envoy.",
                xp=10,
            ),
            Challenge(
                id="ca_circuit_breaker",
                type="text",
                prompt="CIPHER's network shows cascading failures. Describe the Circuit Breaker pattern: what are its three states and how does it prevent cascade failure?",
                answer="closed open half-open",
                explanation="Circuit Breaker has three states: CLOSED (requests flow normally, failures counted), OPEN (requests fail immediately without calling downstream, preventing cascade), HALF-OPEN (limited requests test if downstream recovered). This prevents a failing service from taking down the entire system through resource exhaustion.",
                xp=15,
            ),
        ],
    ),
    "multi_cloud_and_hybrid": Zone(
        id="multi_cloud_and_hybrid",
        title="Multi-Cloud & Hybrid",
        description="Architect systems that span providers and bridge on-prem to cloud.",
        challenges=[
            Challenge(
                id="mc_abstraction_layers",
                type="quiz",
                prompt="CIPHER maps a multi-cloud deployment. The primary purpose of a cloud abstraction layer is:",
                options=[
                    "To make every cloud provider's API identical",
                    "To provide a common interface that minimizes vendor lock-in while accepting some lowest-common-denominator tradeoffs",
                    "To eliminate the need for any cloud-specific configuration",
                    "To automatically migrate workloads between clouds in real time",
                ],
                answer="b",
                explanation="Abstraction layers (like Crossplane, Pulumi, or custom SDKs) provide portable interfaces but always involve tradeoffs. You lose access to provider-specific optimizations. The key is abstracting at the right level — infrastructure primitives, not managed service features.",
                xp=10,
            ),
            Challenge(
                id="mc_terraform_multi",
                type="quiz",
                prompt="When using Terraform for multi-provider deployments, the recommended approach is:",
                options=[
                    "Write one giant .tf file with all providers mixed together",
                    "Use separate provider blocks and state files per cloud, with shared modules for common patterns",
                    "Only use Terraform for AWS; use CloudFormation for the rest",
                    "Avoid using modules to keep things simple",
                ],
                answer="b",
                explanation="Terraform's provider model lets you target AWS, GCP, and Azure in the same codebase. Best practice: separate state per provider/environment, shared modules for abstractions (e.g., a 'compute' module that maps to EC2 or GCE), and workspaces for environment isolation.",
                xp=10,
            ),
            Challenge(
                id="mc_cloud_agnostic_storage",
                type="quiz",
                prompt="CIPHER evaluates storage portability. Which approach is MOST cloud-agnostic for object storage?",
                options=[
                    "Using AWS S3 SDK calls directly throughout the application code",
                    "Implementing an S3-compatible API layer (e.g., MinIO) that works across providers",
                    "Storing all data in a relational database instead",
                    "Using provider-specific CDKs for each cloud",
                ],
                answer="b",
                explanation="The S3 API has become a de facto standard. MinIO, Ceph, GCS (with interoperability), and Azure Blob (with adapters) all support S3-compatible APIs. Code against the S3 interface, swap the endpoint — portable object storage.",
                xp=10,
            ),
            Challenge(
                id="mc_hybrid_connectivity",
                type="quiz",
                prompt="For a hybrid cloud setup connecting on-prem to a public cloud, the most secure and reliable connectivity option is:",
                options=[
                    "A site-to-site VPN over the public internet",
                    "A dedicated private connection like AWS Direct Connect or Azure ExpressRoute",
                    "Opening all ports on the cloud firewall for on-prem IP ranges",
                    "Syncing data manually via USB drives",
                ],
                answer="b",
                explanation="Dedicated connections (Direct Connect, ExpressRoute, Cloud Interconnect) provide consistent bandwidth, lower latency, and traffic that never traverses the public internet. VPN is a cheaper fallback but shares internet bandwidth and has higher latency variance.",
                xp=15,
            ),
            Challenge(
                id="mc_data_sovereignty",
                type="text",
                prompt="CIPHER flags a compliance alert. Explain why data sovereignty matters in multi-cloud architecture and how it constrains cloud region selection.",
                answer="data must reside in specific geographic jurisdictions",
                explanation="Data sovereignty laws (GDPR, China's PIPL, Russia's data localization) require certain data to remain within national borders. This constrains region selection: EU personal data may need eu-west regions, not us-east. Multi-cloud adds complexity — you must verify each provider's region actually has physical infrastructure in the required jurisdiction.",
                xp=15,
            ),
            Challenge(
                id="mc_service_mesh",
                type="quiz",
                prompt="In a multi-cloud microservices deployment, a service mesh (e.g., Istio, Linkerd) primarily provides:",
                options=[
                    "A replacement for Kubernetes entirely",
                    "Transparent mTLS, traffic management, and observability across services regardless of cloud provider",
                    "Automatic database replication between clouds",
                    "A GUI for managing cloud billing",
                ],
                answer="b",
                explanation="A service mesh adds a sidecar proxy to each service, handling mTLS encryption, traffic splitting, retries, circuit breaking, and distributed tracing — all without application code changes. Critical for multi-cloud because it normalizes networking behavior across providers.",
                xp=10,
            ),
            Challenge(
                id="mc_cloud_exit_strategy",
                type="quiz",
                prompt="CIPHER assesses vendor risk. A cloud exit strategy should include:",
                options=[
                    "Nothing — cloud providers never go down or change pricing",
                    "Data export procedures, portable formats, abstraction layers, and documented runbooks for migration",
                    "Simply deleting all cloud resources and starting over",
                    "Only using free-tier services to minimize cost exposure",
                ],
                answer="b",
                explanation="A cloud exit strategy is risk management. Key elements: data in portable formats (not proprietary), infrastructure as code (reproducible elsewhere), documented dependencies on managed services, tested data export procedures, and cost estimates for migration.",
                xp=10,
            ),
            Challenge(
                id="mc_control_plane_placement",
                type="text",
                prompt="CIPHER reviews a multi-cloud architecture diagram. Where should the control plane live in a multi-cloud deployment, and why does this decision matter?",
                answer="control plane placement determines single points of failure and blast radius",
                explanation="The control plane (CI/CD, secrets management, DNS, monitoring) is the brain. Placing it in one cloud creates a dependency — if that provider has an outage, you lose management of all clouds. Options: self-hosted on-prem, distributed across providers, or a dedicated 'management cloud'. The tradeoff is complexity vs. resilience.",
                xp=15,
            ),
        ],
    ),
    "cost_optimization": Zone(
        id="cost_optimization",
        title="Cost Optimization",
        description="Slash cloud spend without sacrificing performance or reliability.",
        challenges=[
            Challenge(
                id="co_reserved_instances",
                type="quiz",
                prompt="CIPHER analyzes the billing dashboard. Reserved Instances (RIs) save money by:",
                options=[
                    "Running instances only during business hours",
                    "Committing to a 1- or 3-year term in exchange for up to 72% discount vs. on-demand pricing",
                    "Using smaller instance types for all workloads",
                    "Sharing instances between multiple AWS accounts",
                ],
                answer="b",
                explanation="Reserved Instances are a billing commitment — you pay for capacity whether you use it or not, in exchange for steep discounts. All Upfront 3-year RIs save the most. Best for steady-state workloads with predictable usage patterns. Savings Plans offer similar discounts with more flexibility.",
                xp=10,
            ),
            Challenge(
                id="co_spot_instances",
                type="quiz",
                prompt="Spot Instances are ideal for:",
                options=[
                    "Production databases that must never restart",
                    "Stateless, fault-tolerant workloads like batch processing, CI/CD, and data analysis",
                    "Any workload — they're just cheaper with no tradeoffs",
                    "Single-instance applications with no redundancy",
                ],
                answer="b",
                explanation="Spot Instances offer up to 90% discount but can be reclaimed with 2 minutes notice. Use them for: batch jobs, map-reduce, CI runners, rendering farms, and stateless web servers behind auto-scaling groups. Never for stateful singleton workloads.",
                xp=10,
            ),
            Challenge(
                id="co_right_sizing",
                type="quiz",
                prompt="CIPHER detects 47 instances averaging 8% CPU utilization. Right-sizing means:",
                options=[
                    "Adding more instances to distribute the load",
                    "Analyzing actual resource usage and matching instance types to real demand — downsizing over-provisioned resources",
                    "Upgrading all instances to the latest generation regardless of need",
                    "Turning off monitoring to reduce overhead",
                ],
                answer="b",
                explanation="Right-sizing is the #1 cost optimization lever. Use CloudWatch metrics, AWS Compute Optimizer, or third-party tools to identify instances where CPU, memory, or network is consistently under-utilized, then downsize. An m5.xlarge at 8% CPU should probably be a t3.medium with burstable credits.",
                xp=10,
            ),
            Challenge(
                id="co_finops_principles",
                type="text",
                prompt="CIPHER requests a FinOps briefing. What are the core principles of FinOps and how does it change the relationship between engineering and finance?",
                answer="FinOps brings financial accountability to cloud spending through collaboration between engineering finance and business",
                explanation="FinOps (Cloud Financial Operations) has three phases: Inform (visibility into who spends what), Optimize (rate and usage optimization), Operate (continuous governance). Core shift: engineers own their cloud costs, finance understands cloud pricing models, and both collaborate on unit economics (cost per transaction, per customer, per feature).",
                xp=15,
            ),
            Challenge(
                id="co_storage_tiering",
                type="quiz",
                prompt="To optimize S3 storage costs for data accessed unpredictably, the best approach is:",
                options=[
                    "Keep everything in S3 Standard forever",
                    "Use S3 Intelligent-Tiering, which automatically moves objects between access tiers based on usage patterns",
                    "Immediately move all data to Glacier Deep Archive",
                    "Delete data older than 30 days",
                ],
                answer="b",
                explanation="S3 Intelligent-Tiering monitors access patterns and moves objects between Frequent, Infrequent, and Archive tiers automatically. No retrieval fees for tier transitions. For predictable patterns, lifecycle policies with explicit transitions to Standard-IA, Glacier, or Deep Archive save more.",
                xp=10,
            ),
            Challenge(
                id="co_tagging_strategy",
                type="quiz",
                prompt="CIPHER audits cost allocation. A robust tagging strategy requires:",
                options=[
                    "Tags are optional and not worth the overhead",
                    "Mandatory tags for cost center, environment, owner, and project — enforced by SCPs or tag policies",
                    "A single 'Name' tag on EC2 instances only",
                    "Using resource names instead of tags for cost tracking",
                ],
                answer="b",
                explanation="Tags are the foundation of cloud cost allocation. Without them, you can't answer 'who spent what on which project?' Enforce mandatory tags (cost-center, env, owner, team, project) via AWS Organizations Tag Policies or SCPs that deny untagged resource creation.",
                xp=10,
            ),
            Challenge(
                id="co_spot_strategy",
                type="quiz",
                prompt="To maximize Spot Instance availability and minimize interruptions, you should:",
                options=[
                    "Use a single instance type in a single AZ",
                    "Diversify across multiple instance types, sizes, and Availability Zones using a capacity-optimized allocation strategy",
                    "Always bid the maximum on-demand price",
                    "Only use Spot during off-peak hours",
                ],
                answer="b",
                explanation="Spot availability varies by instance type, size, and AZ. Diversifying across 10+ instance types and all AZs with capacity-optimized allocation lets the system pick pools with the most available capacity, dramatically reducing interruption rates.",
                xp=15,
            ),
            Challenge(
                id="co_waste_detection",
                type="text",
                prompt="CIPHER scans for cloud waste. List the top 5 categories of cloud waste and how to detect each.",
                answer="idle resources unattached storage over-provisioned instances unused reservations orphaned snapshots",
                explanation="Top cloud waste categories: (1) Idle resources — instances/load balancers with zero traffic; detect via CloudWatch metrics. (2) Unattached EBS volumes — orphaned after instance termination; find with 'available' status filter. (3) Over-provisioned instances — low CPU/memory utilization; detect with Compute Optimizer. (4) Unused Reserved Instances — RIs with no matching usage; check RI utilization reports. (5) Orphaned snapshots — snapshots whose source volume is deleted; audit with age and source-volume checks.",
                xp=15,
            ),
        ],
    ),
    "high_availability": Zone(
        id="high_availability",
        title="High Availability",
        description="Design systems that survive failures at every layer — AZ, region, and beyond.",
        challenges=[
            Challenge(
                id="ha_multi_az",
                type="quiz",
                prompt="CIPHER detects a single-AZ deployment. Deploying across multiple Availability Zones provides:",
                options=[
                    "Better CPU performance for each instance",
                    "Resilience against data center-level failures — if one AZ goes down, traffic shifts to surviving AZs",
                    "Lower costs due to inter-AZ data transfer discounts",
                    "Automatic code deployment to all instances",
                ],
                answer="b",
                explanation="Availability Zones are physically separate data centers within a region, connected by low-latency links. Multi-AZ deployments survive single data center failures — fire, power loss, network partition. This is the minimum for production workloads.",
                xp=10,
            ),
            Challenge(
                id="ha_multi_region",
                type="quiz",
                prompt="Multi-region architecture is necessary when:",
                options=[
                    "You want the absolute lowest latency for users worldwide AND resilience against region-level disasters",
                    "A single AZ is sufficient for all workloads",
                    "You only have users in one country",
                    "You want to avoid using load balancers",
                ],
                answer="a",
                explanation="Multi-region provides two things: geographic proximity (lower latency for global users) and region-level disaster recovery. The tradeoff is significant: data replication complexity, conflict resolution, higher costs, and operational overhead. Only do it when the business requires it.",
                xp=15,
            ),
            Challenge(
                id="ha_rpo_rto",
                type="text",
                prompt="CIPHER needs DR metrics defined. Explain RPO and RTO, what each measures, and how they drive architectural decisions.",
                answer="RPO is maximum acceptable data loss measured in time RTO is maximum acceptable downtime",
                explanation="RPO (Recovery Point Objective): how much data loss is tolerable — e.g., RPO of 1 hour means you need backups/replication at least every hour. RTO (Recovery Time Objective): how quickly you must recover — e.g., RTO of 15 minutes means you need hot standby, not cold restore. Lower RPO/RTO = higher cost. A 0 RPO requires synchronous replication; a 0 RTO requires active-active architecture.",
                xp=15,
            ),
            Challenge(
                id="ha_dr_strategies",
                type="quiz",
                prompt="From cheapest to most expensive, the correct order of DR strategies is:",
                options=[
                    "Multi-site active-active → Warm standby → Pilot light → Backup & restore",
                    "Backup & restore → Pilot light → Warm standby → Multi-site active-active",
                    "Pilot light → Backup & restore → Multi-site active-active → Warm standby",
                    "Warm standby → Multi-site active-active → Backup & restore → Pilot light",
                ],
                answer="b",
                explanation="DR cost spectrum: Backup & Restore (cheapest, highest RTO — restore from backups) → Pilot Light (core infra running, scale up on failover) → Warm Standby (scaled-down copy always running) → Multi-Site Active-Active (full capacity in both regions, lowest RTO/RPO, highest cost).",
                xp=15,
            ),
            Challenge(
                id="ha_health_checks",
                type="quiz",
                prompt="CIPHER configures health checks. A deep health check should verify:",
                options=[
                    "Only that the HTTP port returns 200",
                    "Application logic, database connectivity, dependent service availability, and disk space — not just that the process is running",
                    "Only CPU usage is below 80%",
                    "That the instance was launched less than 24 hours ago",
                ],
                answer="b",
                explanation="Shallow health checks (TCP port open, HTTP 200) miss application-level failures. Deep health checks verify the full dependency chain: can the app query the database? Is the cache reachable? Is disk space adequate? This enables load balancers and orchestrators to make informed routing decisions.",
                xp=10,
            ),
            Challenge(
                id="ha_chaos_engineering",
                type="quiz",
                prompt="Chaos engineering improves availability by:",
                options=[
                    "Randomly destroying production data to test backups",
                    "Proactively injecting controlled failures to discover weaknesses before real outages expose them",
                    "Running all services in debug mode permanently",
                    "Disabling auto-scaling to test manual intervention",
                ],
                answer="b",
                explanation="Chaos engineering (Netflix's Chaos Monkey, AWS Fault Injection Simulator, Gremlin) tests resilience by injecting failures: kill instances, add latency, drop packets, fill disks. The goal: verify that your redundancy actually works under real failure conditions, not just in architecture diagrams.",
                xp=10,
            ),
            Challenge(
                id="ha_data_replication",
                type="quiz",
                prompt="CIPHER evaluates replication lag. Synchronous replication vs. asynchronous replication — the key tradeoff is:",
                options=[
                    "Synchronous is always faster",
                    "Synchronous guarantees zero data loss (RPO=0) but adds write latency; asynchronous has lower latency but risks data loss during failover",
                    "Asynchronous replication costs more",
                    "There is no practical difference for most workloads",
                ],
                answer="b",
                explanation="Synchronous replication: every write confirmed by replica before acknowledging to client. Zero data loss, but write latency = network round-trip to replica. Asynchronous: write acknowledged immediately, replicated in background. Fast writes, but failover may lose seconds of data. Choose based on RPO requirements.",
                xp=10,
            ),
            Challenge(
                id="ha_failover_testing",
                type="text",
                prompt="CIPHER mandates DR validation. Describe a game day exercise: what it is, what you test, and why most teams skip it.",
                answer="game day is a planned exercise simulating real failures to validate DR procedures",
                explanation="A game day deliberately triggers failure scenarios (region failover, database corruption, key rotation) with the team responding in real time. You test: Does automated failover work? Do runbooks match reality? Can the team execute under pressure? Teams skip them because they're scary, time-consuming, and expose uncomfortable gaps. But untested DR plans are fiction.",
                xp=15,
            ),
        ],
    ),
    "well_architected_framework": Zone(
        id="well_architected_framework",
        title="Well-Architected Framework",
        description="Apply the five pillars that separate fragile systems from battle-hardened ones.",
        challenges=[
            Challenge(
                id="wa_operational_excellence",
                type="quiz",
                prompt="CIPHER reviews operational maturity. The Operational Excellence pillar focuses on:",
                options=[
                    "Minimizing cloud costs above all else",
                    "Running and monitoring workloads effectively — IaC, observability, small frequent changes, and learning from failures",
                    "Encrypting all data at rest and in transit",
                    "Using the largest possible instance types for headroom",
                ],
                answer="b",
                explanation="Operational Excellence: perform operations as code (IaC), make frequent small reversible changes, anticipate failure, refine procedures, and learn from all operational events. It's about the human and process side — runbooks, incident response, post-mortems, and continuous improvement.",
                xp=10,
            ),
            Challenge(
                id="wa_security_pillar",
                type="quiz",
                prompt="The Security pillar's defense-in-depth approach means:",
                options=[
                    "Using one very strong firewall and trusting everything behind it",
                    "Applying security controls at every layer — identity, network, application, data — so no single failure compromises the system",
                    "Only encrypting the database; the network is trusted",
                    "Relying entirely on the cloud provider's shared responsibility model",
                ],
                answer="b",
                explanation="Defense in depth: IAM policies + VPC security groups + NACLs + WAF + encryption at rest + encryption in transit + application-level auth + audit logging. Each layer independently limits blast radius. If one layer fails, others still protect the system.",
                xp=10,
            ),
            Challenge(
                id="wa_reliability_pillar",
                type="quiz",
                prompt="CIPHER tests system resilience. The Reliability pillar requires:",
                options=[
                    "Over-provisioning everything by 10x to handle any possible spike",
                    "Automatic recovery from failure, horizontal scaling, and tested disaster recovery procedures",
                    "Manual intervention for all failover scenarios",
                    "Running everything in a single Availability Zone for simplicity",
                ],
                answer="b",
                explanation="Reliability: automatically recover from failure (health checks, auto-scaling, multi-AZ), test recovery procedures regularly (game days), scale horizontally to increase aggregate availability, and manage change through automation. The system should heal itself without human intervention for common failure modes.",
                xp=10,
            ),
            Challenge(
                id="wa_performance_pillar",
                type="quiz",
                prompt="The Performance Efficiency pillar recommends:",
                options=[
                    "Always using the most expensive instance type available",
                    "Selecting the right resource types based on workload requirements, using managed services, and experimenting with new technologies",
                    "Avoiding caching to ensure data freshness",
                    "Running all workloads on a single large server",
                ],
                answer="b",
                explanation="Performance Efficiency: democratize advanced technologies (use managed AI/ML services instead of building from scratch), go global in minutes (CloudFront, multi-region), use serverless where appropriate, and experiment more often. Mechanical sympathy — match the resource to the workload profile.",
                xp=10,
            ),
            Challenge(
                id="wa_cost_pillar",
                type="text",
                prompt="CIPHER demands a cost optimization strategy. How does the Cost Optimization pillar differ from simply 'spending less'?",
                answer="cost optimization maximizes business value per dollar spent not just minimizes total spend",
                explanation="Cost Optimization is about maximizing value, not minimizing spend. Spending $0 is not optimal if it means no revenue. Key practices: adopt a consumption model (pay for what you use), measure overall efficiency (revenue per cloud dollar), stop spending on undifferentiated heavy lifting (use managed services), and analyze attribution (which features generate ROI?).",
                xp=15,
            ),
            Challenge(
                id="wa_shared_responsibility",
                type="quiz",
                prompt="In the AWS Shared Responsibility Model, the customer is responsible for:",
                options=[
                    "Physical security of data centers and hardware maintenance",
                    "Security IN the cloud — data encryption, IAM configuration, OS patching on EC2, network/firewall rules",
                    "Patching the hypervisor and managing the global network backbone",
                    "Maintaining the Availability Zone infrastructure",
                ],
                answer="b",
                explanation="AWS secures the cloud (hardware, facilities, managed service infrastructure). You secure what's IN the cloud: your data, IAM policies, security group rules, OS patches, application code, and encryption configuration. The boundary shifts with service type — EC2 (you patch the OS) vs. Lambda (AWS patches the runtime).",
                xp=10,
            ),
            Challenge(
                id="wa_review_process",
                type="quiz",
                prompt="CIPHER schedules architecture reviews. The Well-Architected Review should be conducted:",
                options=[
                    "Only once, before the initial deployment",
                    "Regularly throughout the workload lifecycle — at key milestones and after significant changes",
                    "Only after a major outage",
                    "Never — the framework is just a reference document",
                ],
                answer="b",
                explanation="Well-Architected Reviews are living assessments, not one-time checklists. Conduct them at design time, before launch, after major changes, and periodically (quarterly). AWS provides the Well-Architected Tool in the console to track findings, improvements, and risk posture over time.",
                xp=10,
            ),
            Challenge(
                id="wa_sustainability",
                type="text",
                prompt="CIPHER flags a sixth pillar. The Sustainability pillar was added in 2021. What does it measure and what architectural choices does it influence?",
                answer="sustainability measures environmental impact and drives efficient resource usage to reduce carbon footprint",
                explanation="The Sustainability pillar addresses environmental impact: minimize provisioned resources, maximize utilization, use efficient hardware (Graviton), select regions with renewable energy, and reduce downstream data transfer. Architectural choices: right-size aggressively, use spot/serverless (shared infrastructure = higher utilization), implement data lifecycle policies, and measure carbon per transaction using the AWS Customer Carbon Footprint Tool.",
                xp=15,
            ),
        ],
    ),
}
