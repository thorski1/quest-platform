"""
zones.py — IaC skill pack zones for NEXUS.
"""

from engine.zone import Zone
from engine.challenge import Challenge

ZONE_ORDER = [
    "iac_fundamentals",
    "pulumi_and_cdk",
    "ansible_config_mgmt",
    "gitops_and_argocd",
    "iac_testing_and_security",
]

ZONES = {
    "iac_fundamentals": Zone(
        id="iac_fundamentals",
        title="IaC Fundamentals",
        description="Declarative vs imperative, idempotency, drift detection, and IaC principles.",
        challenges=[
            Challenge(
                id="iac_declarative_vs_imperative",
                type="quiz",
                prompt="What is the fundamental difference between declarative and imperative Infrastructure as Code?",
                options=[
                    "Declarative IaC describes the desired end state; imperative IaC describes the step-by-step commands to reach it",
                    "Declarative IaC is faster; imperative IaC is more secure",
                    "Declarative IaC only works with cloud providers; imperative IaC works on-premise",
                    "There is no real difference — they are marketing terms for the same approach",
                ],
                answer="a",
                explanation="Declarative IaC (Terraform, CloudFormation, Pulumi) says 'I want 3 EC2 instances' and the tool figures out how. Imperative IaC (bash scripts, some Ansible tasks) says 'create instance 1, then create instance 2, then create instance 3.' Declarative is idempotent by nature; imperative requires explicit checks.",
                xp=10,
                hints=["Think 'what' vs 'how'.", "Terraform is a classic example of one approach."],
            ),
            Challenge(
                id="iac_idempotency_def",
                type="quiz",
                prompt="Idempotency in IaC means:",
                options=[
                    "Running the same configuration once is faster than running it twice",
                    "Applying the same configuration multiple times produces the same result as applying it once",
                    "Infrastructure can only be created, never modified or destroyed",
                    "Each resource must have a unique identifier across all environments",
                ],
                answer="b",
                explanation="Idempotency is the property that applying the same operation multiple times yields the same result. If your IaC says '3 servers with 4GB RAM' and you apply it 10 times, you still have 3 servers with 4GB RAM — not 30. This is critical for reliability and repeatability.",
                xp=10,
                hints=["Apply once or ten times — same result.", "It prevents duplicate resource creation."],
            ),
            Challenge(
                id="iac_drift_explain",
                type="fill_blank",
                prompt="When someone manually changes infrastructure that is managed by IaC, causing the actual state to differ from the declared state, this is called configuration ___.",
                answer="drift",
                explanation="Configuration drift occurs when the real infrastructure diverges from what the IaC code declares. Someone clicks through the console, changes a security group rule, resizes an instance — now the code says one thing and reality says another. Detecting and remediating drift is a core IaC operational concern.",
                xp=15,
                hints=["The actual state 'drifts' away from the declared state.", "Terraform plan can detect this."],
            ),
            Challenge(
                id="iac_benefits",
                type="quiz",
                prompt="Which of the following is NOT a primary benefit of Infrastructure as Code?",
                options=[
                    "Version control — infrastructure changes are tracked in Git",
                    "Reproducibility — identical environments can be created reliably",
                    "Guaranteed zero downtime during all deployments",
                    "Auditability — who changed what and when is logged in commit history",
                ],
                answer="c",
                explanation="IaC provides version control, reproducibility, and auditability. It does NOT guarantee zero downtime — that depends on deployment strategies (blue-green, canary, rolling updates). IaC can facilitate zero-downtime patterns, but it does not inherently prevent downtime.",
                xp=10,
                hints=["One of these is about availability, not code management.", "IaC is about how you define infra, not how you deploy apps."],
            ),
            Challenge(
                id="iac_state_purpose",
                type="quiz",
                prompt="Why do many IaC tools maintain a state file or state record?",
                options=[
                    "To cache API responses and speed up subsequent runs",
                    "To map declared resources to real-world objects and track metadata for change detection",
                    "To store secrets and credentials used during provisioning",
                    "To generate documentation for the infrastructure automatically",
                ],
                answer="b",
                explanation="State is the bridge between your code and reality. It maps `aws_instance.web` to `i-0abc123def`. Without state, the tool cannot know which real resources correspond to which code declarations. State also stores attribute values needed for dependency resolution and change detection.",
                xp=10,
                hints=["It connects code names to real resource IDs.", "Without it, the tool wouldn't know what already exists."],
            ),
            Challenge(
                id="iac_immutable_vs_mutable",
                type="quiz",
                prompt="What distinguishes immutable infrastructure from mutable infrastructure?",
                options=[
                    "Immutable infrastructure is cheaper to run than mutable infrastructure",
                    "Immutable infrastructure replaces servers entirely on change; mutable infrastructure modifies servers in place",
                    "Immutable infrastructure cannot be deleted; mutable infrastructure can",
                    "Immutable infrastructure uses containers; mutable infrastructure uses VMs",
                ],
                answer="b",
                explanation="Immutable infrastructure treats servers as disposable: instead of SSH-ing in and patching, you build a new image, deploy new instances, and destroy the old ones. Mutable infrastructure updates in place (apt upgrade, config file changes). Immutability eliminates drift and snowflake servers but requires robust image pipelines.",
                xp=10,
                hints=["Think 'replace' vs 'patch'.", "One approach never changes a running server."],
            ),
            Challenge(
                id="iac_convergence",
                type="fill_blank",
                prompt="The process by which a configuration management tool brings a system from its current state to the desired state is called ___.",
                answer="convergence",
                explanation="Convergence is when a tool evaluates the current state, compares it to the desired state, and takes only the actions needed to close the gap. Chef and Puppet are convergence-based: they run periodically and 'converge' the system toward the declared state, fixing any drift.",
                xp=15,
                hints=["The system 'converges' toward the desired state.", "Chef and Puppet use this model."],
            ),
            Challenge(
                id="iac_fundamentals_boss",
                type="quiz",
                prompt="An engineer runs `terraform apply` and the plan shows '0 to add, 0 to change, 0 to destroy.' They then SSH into the server and manually change the instance type from t3.micro to t3.large. The next `terraform plan` will show:",
                options=[
                    "0 changes — Terraform doesn't detect manual changes",
                    "1 to change — Terraform will propose reverting the instance type back to t3.micro",
                    "1 to destroy, 1 to add — Terraform always recreates on any change",
                    "An error — Terraform cannot reconcile manual changes",
                ],
                answer="b",
                explanation="Terraform refreshes real-world state on each plan. It detects the drift (instance_type changed from t3.micro to t3.large) and proposes changing it back to match the code (t3.micro). This is drift detection and remediation in action — the code is the source of truth, not the console.",
                xp=25,
                hints=["Terraform compares code to reality on every plan.", "The code still says t3.micro."],
                is_boss=True,
            ),
        ],
    ),
    "pulumi_and_cdk": Zone(
        id="pulumi_and_cdk",
        title="Pulumi & CDK",
        description="TypeScript IaC, constructs, stacks, and programmatic infrastructure.",
        challenges=[
            Challenge(
                id="iac_pulumi_language",
                type="quiz",
                prompt="What makes Pulumi fundamentally different from Terraform in how you write infrastructure definitions?",
                options=[
                    "Pulumi uses YAML exclusively while Terraform uses HCL",
                    "Pulumi lets you use general-purpose programming languages (TypeScript, Python, Go) instead of a domain-specific language",
                    "Pulumi doesn't need a state file while Terraform does",
                    "Pulumi only works with AWS while Terraform is multi-cloud",
                ],
                answer="b",
                explanation="Pulumi's core differentiator is using real programming languages. You write infrastructure in TypeScript, Python, Go, C#, or Java — with full access to loops, conditionals, classes, packages, and testing frameworks. No DSL to learn. The trade-off: you need to know a programming language well.",
                xp=10,
                hints=["Think about the language you write infrastructure in.", "One uses a DSL, the other uses real code."],
            ),
            Challenge(
                id="iac_cdk_construct",
                type="quiz",
                prompt="In AWS CDK, a 'construct' is:",
                options=[
                    "A deployment pipeline stage that runs CloudFormation",
                    "A cloud component that represents one or more AWS resources, composable into higher-level abstractions",
                    "A security policy that restricts which resources can be created",
                    "A monitoring dashboard template for CloudWatch",
                ],
                answer="b",
                explanation="Constructs are the building blocks of CDK. L1 constructs map 1:1 to CloudFormation resources. L2 constructs add sensible defaults and convenience methods. L3 constructs (patterns) compose multiple resources into common architectures. You build infrastructure by composing constructs like Lego blocks.",
                xp=10,
                hints=["Think of them as building blocks.", "They come in three levels of abstraction."],
            ),
            Challenge(
                id="iac_pulumi_stack",
                type="fill_blank",
                prompt="In Pulumi, the isolated deployment environment that has its own configuration and state (similar to a Terraform workspace) is called a ___.",
                answer="stack",
                explanation="A Pulumi stack is an independently configurable instance of a Pulumi program. You might have stacks named 'dev', 'staging', and 'prod' — each with its own config values and state. Stacks are the unit of deployment and isolation in Pulumi, analogous to Terraform workspaces.",
                xp=15,
                hints=["It's Pulumi's equivalent of a Terraform workspace.", "Think of a stack of environments."],
            ),
            Challenge(
                id="iac_cdk_synth",
                type="quiz",
                prompt="What does `cdk synth` do in the AWS CDK workflow?",
                options=[
                    "Deploys all stacks to AWS immediately",
                    "Synthesizes the CDK app into a CloudFormation template without deploying",
                    "Destroys all resources created by the CDK app",
                    "Runs unit tests against the CDK constructs",
                ],
                answer="b",
                explanation="`cdk synth` compiles your CDK code (TypeScript, Python, etc.) into CloudFormation JSON/YAML templates. It's the 'compile' step — it catches errors in your constructs before any AWS API call is made. The output goes to the `cdk.out` directory. Always synth before deploy.",
                xp=10,
                hints=["Synth is short for synthesize.", "It produces CloudFormation templates."],
            ),
            Challenge(
                id="iac_pulumi_up",
                type="quiz",
                prompt="The Pulumi command equivalent to `terraform apply` is:",
                options=[
                    "pulumi deploy",
                    "pulumi up",
                    "pulumi apply",
                    "pulumi push",
                ],
                answer="b",
                explanation="`pulumi up` previews changes (like `terraform plan`) and then applies them (like `terraform apply`) in a single interactive command. It shows a diff, asks for confirmation, and then provisions/updates/deletes resources. Use `pulumi up --yes` to skip confirmation (like `terraform apply -auto-approve`).",
                xp=10,
                hints=["It's a two-letter subcommand.", "It goes 'up' — as in, bring the infrastructure up."],
            ),
            Challenge(
                id="iac_cdk_l2_construct",
                type="quiz",
                prompt="What advantage does an L2 construct in AWS CDK have over an L1 construct?",
                options=[
                    "L2 constructs are faster to deploy than L1 constructs",
                    "L2 constructs provide sensible defaults, helper methods, and reduce boilerplate compared to raw CloudFormation mappings",
                    "L2 constructs bypass CloudFormation entirely and call AWS APIs directly",
                    "L2 constructs are the only constructs that support IAM policies",
                ],
                answer="b",
                explanation="L1 constructs (CfnXxx) are auto-generated 1:1 CloudFormation mappings — verbose and low-level. L2 constructs add defaults (e.g., encryption enabled, versioning on), convenience methods (bucket.grantRead(lambda)), and type safety. They dramatically reduce boilerplate while remaining fully customizable.",
                xp=10,
                hints=["L2 adds convenience on top of the raw CloudFormation mapping.", "Think defaults and helper methods."],
            ),
            Challenge(
                id="iac_pulumi_output",
                type="fill_blank",
                prompt="In Pulumi, when a resource property isn't known until deployment time (like an auto-generated ARN), it is represented as an ___ type that resolves asynchronously.",
                answer="output",
                explanation="Pulumi Outputs are values that are resolved asynchronously during deployment. Since resource properties like ARNs, IDs, and endpoints aren't known until the cloud provider creates the resource, Pulumi wraps them in Output<T>. You chain operations with `.apply()` — similar to Promises in JavaScript.",
                xp=15,
                hints=["It's the same word Terraform uses for exported values.", "Think async/Promise-like wrapper."],
            ),
            Challenge(
                id="iac_pulumi_cdk_boss",
                type="quiz",
                prompt="A team writes this Pulumi TypeScript code:\n\n```typescript\nconst bucket = new aws.s3.Bucket('data', {\n  versioning: { enabled: true },\n});\nexport const bucketName = bucket.id;\n```\n\nWhat will `bucketName` contain after `pulumi up`?",
                options=[
                    "The literal string 'data'",
                    "The auto-generated AWS bucket name (e.g., 'data-a1b2c3d')",
                    "undefined — bucket.id is not a valid property",
                    "The Pulumi URN of the bucket resource",
                ],
                answer="b",
                explanation="In Pulumi, the first argument ('data') is the logical name used by Pulumi for tracking. The actual AWS bucket name is auto-generated with a random suffix to ensure uniqueness (e.g., 'data-a1b2c3d'). `bucket.id` returns this physical AWS name. To set an exact name, you'd use the `bucket` property explicitly.",
                xp=25,
                hints=["Pulumi auto-generates physical names with a random suffix.", "The first argument is the logical name, not the physical name."],
                is_boss=True,
            ),
        ],
    ),
    "ansible_config_mgmt": Zone(
        id="ansible_config_mgmt",
        title="Ansible & Configuration Management",
        description="Playbooks, roles, inventory, idempotency, and server configuration.",
        challenges=[
            Challenge(
                id="iac_ansible_agentless",
                type="quiz",
                prompt="Ansible is described as 'agentless.' What does this mean?",
                options=[
                    "Ansible doesn't use any configuration files",
                    "Ansible connects to remote hosts via SSH (or WinRM) without requiring a pre-installed agent daemon on the target",
                    "Ansible can only manage local resources, not remote servers",
                    "Ansible doesn't require an internet connection to operate",
                ],
                answer="b",
                explanation="Unlike Chef or Puppet, which require agent software running on every managed host, Ansible pushes commands over SSH (Linux) or WinRM (Windows). No daemon to install, no ports to open, no agent to update. This simplifies bootstrapping — if you can SSH to a host, you can manage it with Ansible.",
                xp=10,
                hints=["No software needs to be pre-installed on the target.", "It uses SSH."],
            ),
            Challenge(
                id="iac_ansible_playbook",
                type="quiz",
                prompt="An Ansible playbook is:",
                options=[
                    "A binary executable that Ansible compiles from YAML",
                    "A YAML file containing one or more plays, each mapping a set of hosts to a set of tasks",
                    "A Python script that directly calls the Ansible API",
                    "A Jinja2 template used to generate configuration files",
                ],
                answer="b",
                explanation="A playbook is a YAML file with a list of plays. Each play specifies which hosts to target (from inventory) and which tasks to execute. Tasks call modules (apt, copy, service, etc.) in order. Playbooks are the core unit of orchestration in Ansible — they're human-readable automation runbooks.",
                xp=10,
                hints=["It's written in YAML.", "It maps hosts to tasks."],
            ),
            Challenge(
                id="iac_ansible_inventory",
                type="fill_blank",
                prompt="The file that defines the list of managed hosts (grouped by role, environment, or function) that Ansible will target is called the ___.",
                answer="inventory",
                explanation="The inventory defines what Ansible can manage. It can be a static INI/YAML file listing hosts and groups, or a dynamic inventory script/plugin that queries AWS, Azure, or a CMDB. Groups like [webservers], [databases], [production] let you target subsets of your infrastructure.",
                xp=15,
                hints=["It's a list of all managed hosts and groups.", "It can be static (file) or dynamic (script/API)."],
            ),
            Challenge(
                id="iac_ansible_role",
                type="quiz",
                prompt="An Ansible role is:",
                options=[
                    "An IAM permission set that controls what Ansible can do",
                    "A reusable, self-contained unit of automation with its own tasks, handlers, templates, variables, and defaults in a standard directory structure",
                    "A single task within a playbook",
                    "A remote user account that Ansible uses to SSH into hosts",
                ],
                answer="b",
                explanation="Roles are Ansible's packaging mechanism. A role has a standard directory layout: tasks/, handlers/, templates/, files/, vars/, defaults/, meta/. This makes automation reusable and shareable. You include roles in playbooks with `roles:` or `include_role`. Ansible Galaxy hosts thousands of community roles.",
                xp=10,
                hints=["It's a way to package and reuse automation.", "It has a standard directory structure."],
            ),
            Challenge(
                id="iac_ansible_idempotent",
                type="quiz",
                prompt="Which Ansible task is idempotent?",
                options=[
                    "command: echo 'hello' >> /tmp/log.txt",
                    "apt: name=nginx state=present",
                    "shell: curl https://example.com/setup.sh | bash",
                    "raw: rm -rf /tmp/cache",
                ],
                answer="b",
                explanation="The `apt` module with `state=present` checks if nginx is installed first. If it is, it does nothing. If not, it installs it. Running it 10 times = same result. The command/shell/raw modules execute blindly every time — appending to a file, re-running a script, deleting a directory — they're not idempotent without explicit guards.",
                xp=10,
                hints=["Idempotent means same result no matter how many times you run it.", "Modules with 'state' parameters tend to be idempotent."],
            ),
            Challenge(
                id="iac_ansible_handler",
                type="quiz",
                prompt="In Ansible, a handler is:",
                options=[
                    "A task that runs on every execution regardless of changes",
                    "A task that is triggered only when notified by another task that reported a change",
                    "A special variable that stores the return value of a module",
                    "A callback plugin that logs task results to an external system",
                ],
                answer="b",
                explanation="Handlers are tasks that only run when notified. Classic pattern: a task updates a config file and notifies a handler to restart the service. If the config didn't change (task reports ok, not changed), the handler doesn't fire. This prevents unnecessary service restarts — only restart when something actually changed.",
                xp=10,
                hints=["They respond to changes, not every run.", "Common use: restart a service after config changes."],
            ),
            Challenge(
                id="iac_ansible_jinja2",
                type="fill_blank",
                prompt="Ansible uses the ___ templating engine (from Python) to dynamically generate configuration files and evaluate expressions in playbooks.",
                answer="jinja2",
                explanation="Jinja2 is the templating engine behind Ansible's {{ variable }} syntax. It powers template files (.j2), conditional expressions (when: ansible_os_family == 'Debian'), and loop expressions. Understanding Jinja2 filters (| default, | join, | regex_replace) is essential for advanced Ansible usage.",
                xp=15,
                hints=["It's a Python templating engine.", "Its templates use {{ double curly braces }}."],
            ),
            Challenge(
                id="iac_ansible_boss",
                type="quiz",
                prompt="An Ansible playbook runs this task:\n\n```yaml\n- name: Deploy config\n  template:\n    src: app.conf.j2\n    dest: /etc/app/app.conf\n  notify: restart app\n```\n\nThe task runs twice. First run: the template output differs from the existing file. Second run: the template output matches the existing file. What happens with the handler?",
                options=[
                    "The handler runs both times",
                    "The handler runs on the first run (file changed) but not the second (file unchanged)",
                    "The handler never runs because template module doesn't support notify",
                    "The handler runs on the second run only, after confirming the file is correct",
                ],
                answer="b",
                explanation="On the first run, the template module detects the file content differs, writes the new content, reports 'changed', and triggers the notify. On the second run, the rendered template matches the existing file, so the module reports 'ok' (not changed) and the handler is not notified. This is idempotency + handler notification working together.",
                xp=25,
                hints=["Handlers only fire when the notifying task reports 'changed'.", "If the file already matches, no change is reported."],
                is_boss=True,
            ),
        ],
    ),
    "gitops_and_argocd": Zone(
        id="gitops_and_argocd",
        title="GitOps & ArgoCD",
        description="Git as source of truth, sync loops, reconciliation, and declarative delivery.",
        challenges=[
            Challenge(
                id="iac_gitops_principle",
                type="quiz",
                prompt="The core principle of GitOps is:",
                options=[
                    "All application code must be stored in GitHub specifically",
                    "Git is the single source of truth for declarative infrastructure and application state, with automated reconciliation",
                    "Developers must use git rebase instead of git merge for all changes",
                    "CI/CD pipelines must be written in YAML and stored in .github/ directories",
                ],
                answer="b",
                explanation="GitOps declares that Git is the source of truth. The desired state of infrastructure and applications lives in Git. An operator (like ArgoCD or Flux) continuously compares the Git-declared state to the actual cluster state and reconciles any differences. Push to Git, the cluster converges. No kubectl apply by hand.",
                xp=10,
                hints=["Git is the source of truth.", "An operator watches Git and reconciles."],
            ),
            Challenge(
                id="iac_gitops_pull_vs_push",
                type="quiz",
                prompt="What distinguishes a pull-based GitOps deployment from a traditional push-based CI/CD pipeline?",
                options=[
                    "Pull-based is slower but more reliable",
                    "In pull-based, an in-cluster agent pulls desired state from Git; in push-based, the CI server pushes changes to the cluster",
                    "Pull-based only works with Kubernetes; push-based works with any platform",
                    "Pull-based requires manual approval; push-based is fully automated",
                ],
                answer="b",
                explanation="Push-based: CI server runs `kubectl apply` or `helm upgrade` — the CI server needs cluster credentials. Pull-based: an agent inside the cluster (ArgoCD, Flux) watches a Git repo and pulls changes. The agent already has cluster access; no credentials leave the cluster. This is more secure and self-healing.",
                xp=10,
                hints=["Who initiates the deployment — the CI server or an in-cluster agent?", "Think about where the credentials live."],
            ),
            Challenge(
                id="iac_argocd_app",
                type="fill_blank",
                prompt="In ArgoCD, the custom resource that defines which Git repository, path, and target cluster/namespace to sync is called an ___.",
                answer="application",
                explanation="An ArgoCD Application CRD specifies: source (Git repo URL + path + target revision), destination (cluster + namespace), and sync policy. It's the unit of deployment in ArgoCD. One Application = one set of manifests from one Git path deployed to one namespace. You can also use ApplicationSets for multi-cluster/multi-tenant patterns.",
                xp=15,
                hints=["It's the primary custom resource in ArgoCD.", "It maps a Git path to a Kubernetes namespace."],
            ),
            Challenge(
                id="iac_argocd_sync",
                type="quiz",
                prompt="When ArgoCD detects that the live cluster state differs from the Git-declared state, the application status shows:",
                options=[
                    "Healthy",
                    "OutOfSync",
                    "Degraded",
                    "Unknown",
                ],
                answer="b",
                explanation="ArgoCD continuously compares the desired state (Git) with the live state (cluster). When they differ — someone ran kubectl edit, a resource was deleted, an image tag changed in Git — the app shows OutOfSync. You then sync (manually or auto-sync) to reconcile. 'Healthy' is about resource health (pods running); 'OutOfSync' is about state divergence.",
                xp=10,
                hints=["The live state is 'out of' alignment with the declared state.", "It's a two-word status."],
            ),
            Challenge(
                id="iac_argocd_self_heal",
                type="quiz",
                prompt="ArgoCD's 'self-heal' feature does what?",
                options=[
                    "Automatically restarts failed pods in the cluster",
                    "Automatically reverts manual changes to cluster resources by re-syncing from Git",
                    "Sends alerts when a deployment fails",
                    "Automatically updates the Git repository when cluster state changes",
                ],
                answer="b",
                explanation="Self-heal means ArgoCD continuously enforces the Git state. If someone manually edits a Deployment (kubectl edit), ArgoCD detects the drift and automatically syncs the resource back to match Git. Git always wins. This prevents configuration drift and enforces the GitOps contract: Git is the only way to make changes.",
                xp=10,
                hints=["It automatically fixes drift.", "Manual changes get overwritten."],
            ),
            Challenge(
                id="iac_gitops_reconciliation",
                type="fill_blank",
                prompt="The continuous process by which a GitOps operator compares desired state in Git with actual state in the cluster and corrects any differences is called ___.",
                answer="reconciliation",
                explanation="Reconciliation is the core loop of GitOps. The operator polls Git (or receives webhooks), compares the declared manifests to the live cluster state, and takes action to converge. This happens continuously — not just on push. If a resource drifts (manual change, controller mutation), the reconciliation loop catches and fixes it.",
                xp=15,
                hints=["It's the process of making reality match the declaration.", "It's a continuous loop."],
            ),
            Challenge(
                id="iac_argocd_app_of_apps",
                type="quiz",
                prompt="The 'App of Apps' pattern in ArgoCD is used to:",
                options=[
                    "Deploy a single application across multiple clusters simultaneously",
                    "Create a parent Application that manages child Application resources, enabling hierarchical deployment management",
                    "Run multiple versions of the same application side by side",
                    "Automatically generate ArgoCD Applications from Helm chart dependencies",
                ],
                answer="b",
                explanation="App of Apps is a bootstrap pattern: one root Application points to a Git directory containing other Application manifests. When ArgoCD syncs the root app, it creates the child apps, which then sync their own repos/paths. This enables managing dozens of applications from a single entry point and is the foundation of fleet management.",
                xp=10,
                hints=["It's a hierarchical pattern.", "One parent app manages many child apps."],
            ),
            Challenge(
                id="iac_gitops_boss",
                type="quiz",
                prompt="A team uses ArgoCD with auto-sync and self-heal enabled. A developer runs `kubectl scale deployment/api --replicas=5` directly against the cluster. The Git manifest declares `replicas: 3`. What happens?",
                options=[
                    "The deployment stays at 5 replicas permanently",
                    "ArgoCD detects the drift and scales the deployment back to 3 replicas to match Git",
                    "ArgoCD updates the Git repository to say replicas: 5",
                    "ArgoCD disables auto-sync and alerts the team",
                ],
                answer="b",
                explanation="With self-heal enabled, ArgoCD's reconciliation loop detects that the live state (5 replicas) differs from Git (3 replicas). It automatically syncs, overwriting the manual change and scaling back to 3. Git is the source of truth — the only way to scale to 5 is to change the manifest in Git and let ArgoCD sync it. This is GitOps enforcement.",
                xp=25,
                hints=["Self-heal means Git always wins.", "ArgoCD will revert manual changes."],
                is_boss=True,
            ),
        ],
    ),
    "iac_testing_and_security": Zone(
        id="iac_testing_and_security",
        title="IaC Testing & Security",
        description="Checkov, tfsec, policy as code, OPA, and infrastructure testing.",
        challenges=[
            Challenge(
                id="iac_shift_left",
                type="quiz",
                prompt="In the context of IaC security, 'shift left' means:",
                options=[
                    "Moving infrastructure to a different AWS region",
                    "Catching security misconfigurations early in the development pipeline, before deployment",
                    "Rolling back to a previous version of the infrastructure",
                    "Migrating from cloud to on-premise infrastructure",
                ],
                answer="b",
                explanation="Shift left means moving security checks earlier (leftward) in the development lifecycle. Instead of finding a public S3 bucket after deployment, you catch it in the PR when the IaC code is reviewed. Tools like Checkov, tfsec, and OPA run in CI before apply, preventing insecure infrastructure from ever being provisioned.",
                xp=10,
                hints=["'Left' refers to earlier in the development pipeline.", "Catch problems before deployment, not after."],
            ),
            Challenge(
                id="iac_checkov_what",
                type="quiz",
                prompt="Checkov is a static analysis tool that:",
                options=[
                    "Compiles Terraform code into machine-readable bytecode",
                    "Scans IaC files (Terraform, CloudFormation, Kubernetes, etc.) for security and compliance misconfigurations before deployment",
                    "Monitors running infrastructure for security vulnerabilities in real time",
                    "Generates IaC code from existing cloud infrastructure",
                ],
                answer="b",
                explanation="Checkov scans IaC files against hundreds of built-in policies. It checks for public S3 buckets, unencrypted databases, overly permissive security groups, missing logging, and more. It supports Terraform, CloudFormation, Kubernetes YAML, Helm, Dockerfile, and ARM templates. It runs in CI/CD before any infrastructure is created.",
                xp=10,
                hints=["It's a scanner, not a provisioner.", "It checks IaC files for security issues."],
            ),
            Challenge(
                id="iac_tfsec_purpose",
                type="fill_blank",
                prompt="The Terraform-specific static analysis security scanner (now part of Trivy) that checks .tf files for security misconfigurations is called ___.",
                answer="tfsec",
                explanation="tfsec (now integrated into Aqua Security's Trivy) scans Terraform HCL files for security issues. It understands Terraform-specific constructs like module references, variable defaults, and resource attribute relationships. Example findings: aws_s3_bucket without encryption, aws_security_group with 0.0.0.0/0 ingress, RDS without backup enabled.",
                xp=15,
                hints=["It's named after the tool (tf) and what it checks (sec).", "It's now part of Trivy."],
            ),
            Challenge(
                id="iac_opa_what",
                type="quiz",
                prompt="Open Policy Agent (OPA) is:",
                options=[
                    "A cloud-native firewall that blocks unauthorized API calls",
                    "A general-purpose policy engine that decouples policy decisions from application logic, using the Rego language",
                    "An authentication provider for Kubernetes service accounts",
                    "A Terraform provider for managing IAM policies",
                ],
                answer="b",
                explanation="OPA is a general-purpose policy engine. You write policies in Rego (a declarative query language), feed OPA structured data (JSON), and it returns policy decisions. For IaC: OPA evaluates Terraform plans against policies ('no public S3 buckets', 'all instances must be tagged'). It decouples policy from tools — one engine, many use cases.",
                xp=10,
                hints=["It's a general-purpose engine, not tied to one tool.", "It uses the Rego language."],
            ),
            Challenge(
                id="iac_policy_as_code",
                type="quiz",
                prompt="What problem does 'Policy as Code' solve that manual review cannot?",
                options=[
                    "It makes infrastructure faster to provision",
                    "It enforces security and compliance rules automatically, consistently, and at scale across every change",
                    "It eliminates the need for any security team involvement",
                    "It guarantees 100% uptime for all infrastructure",
                ],
                answer="b",
                explanation="Humans miss things. A reviewer might catch a public S3 bucket in one PR but miss it in another. Policy as Code automates these checks: every PR, every plan, every deployment is evaluated against the same rules. No exceptions, no fatigue, no missed reviews. It scales from 1 PR/day to 1000.",
                xp=10,
                hints=["Think consistency and scale.", "Automated enforcement vs manual review."],
            ),
            Challenge(
                id="iac_terratest",
                type="quiz",
                prompt="Terratest is a Go library used to:",
                options=[
                    "Generate Terraform code from templates",
                    "Write automated tests that deploy real infrastructure, validate it works, and then destroy it",
                    "Lint Terraform HCL files for syntax errors",
                    "Convert Terraform state files to human-readable reports",
                ],
                answer="b",
                explanation="Terratest deploys real infrastructure in a sandbox environment, runs validation (HTTP requests, SSH commands, API calls), and tears it down. It's integration testing for IaC. Example: deploy a VPC module, verify the CIDR block is correct, check the NAT gateway is reachable, destroy everything. Slow but thorough — catches issues unit tests cannot.",
                xp=10,
                hints=["It tests real infrastructure, not just code.", "It's an integration testing framework."],
            ),
            Challenge(
                id="iac_sentinel",
                type="fill_blank",
                prompt="HashiCorp's embedded policy-as-code framework that integrates natively with Terraform Cloud/Enterprise to enforce rules on terraform plans is called ___.",
                answer="sentinel",
                explanation="Sentinel is HashiCorp's policy-as-code framework, built into Terraform Cloud and Enterprise. Policies are written in the Sentinel language and evaluated between plan and apply. If a policy fails, the apply is blocked. Example: 'all S3 buckets must have encryption enabled' — checked on every run, enforced before any resource is created.",
                xp=15,
                hints=["It's HashiCorp's own policy framework.", "It stands guard between plan and apply."],
            ),
            Challenge(
                id="iac_testing_boss",
                type="quiz",
                prompt="A security team implements this OPA Rego policy:\n\n```rego\npackage terraform\n\ndeny[msg] {\n  resource := input.planned_values.root_module.resources[_]\n  resource.type == \"aws_s3_bucket\"\n  not resource.values.server_side_encryption_configuration\n  msg := sprintf(\"S3 bucket '%s' must have encryption enabled\", [resource.name])\n}\n```\n\nA developer submits a Terraform plan with an unencrypted S3 bucket. What happens?",
                options=[
                    "The policy ignores the bucket because OPA can't read Terraform plans",
                    "The policy generates a deny message identifying the unencrypted bucket, and the CI pipeline blocks the apply",
                    "The policy automatically adds encryption to the bucket configuration",
                    "The policy logs a warning but allows the apply to proceed",
                ],
                answer="b",
                explanation="The Rego policy iterates over all planned resources, finds S3 buckets without encryption config, and generates a deny message. In a CI/CD pipeline, any deny message means the policy check fails, blocking the terraform apply. The developer must add encryption to the bucket config and re-submit. This is policy-as-code enforcement — automated, consistent, and non-negotiable.",
                xp=25,
                hints=["The deny rule produces a message when the condition is true.", "A non-empty deny set means policy violation."],
                is_boss=True,
            ),
        ],
    ),
}
