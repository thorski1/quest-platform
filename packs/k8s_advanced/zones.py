"""
zones.py — Advanced Kubernetes skill pack zones for NEXUS.
"""

from engine.zone import Zone
from engine.challenge import Challenge

ZONE_ORDER = [
    "custom_resources_operators",
    "networking_deep_dive",
    "storage_statefulsets",
    "security_rbac",
    "cluster_operations",
]

ZONES = {
    # ── Zone 1: Custom Resources & Operators ──────────────────────────
    "custom_resources_operators": Zone(
        id="custom_resources_operators",
        title="Custom Resources & Operators",
        description="CRDs, controller-runtime, the operator pattern, finalizers, and owner references.",
        challenges=[
            Challenge(
                id="k8sa_crd_purpose",
                type="quiz",
                prompt="What does a CustomResourceDefinition (CRD) allow you to do in Kubernetes?",
                options=[
                    "Override built-in resource types like Pod and Service",
                    "Define entirely new resource types with custom schemas that the API server will manage",
                    "Add custom fields to existing resources like Deployments",
                    "Create new API servers that run alongside the main one",
                ],
                answer="b",
                explanation="A CRD extends the Kubernetes API by registering a new resource type. Once applied, you can create, read, update, and delete instances of that custom resource using kubectl, just like built-in resources.",
                xp=10,
            ),
            Challenge(
                id="k8sa_crd_scope",
                type="quiz",
                prompt="A CRD has `scope: Namespaced` vs `scope: Cluster`. What does this control?",
                options=[
                    "Whether the CRD itself can be edited by namespace admins",
                    "Whether instances of the custom resource live in a namespace or are cluster-wide",
                    "Whether the CRD is replicated across control-plane nodes",
                    "Whether RBAC rules apply to the custom resource",
                ],
                answer="b",
                explanation="The `scope` field determines whether custom resource instances are namespaced (like Pods) or cluster-scoped (like Nodes). Namespaced CRs are isolated per namespace; cluster-scoped CRs are global.",
                xp=10,
            ),
            Challenge(
                id="k8sa_operator_pattern",
                type="quiz",
                prompt="The Kubernetes operator pattern combines which two things?",
                options=[
                    "A CRD and a controller that watches it",
                    "A Helm chart and a CI/CD pipeline",
                    "A StatefulSet and a PersistentVolume",
                    "An Admission Webhook and a NetworkPolicy",
                ],
                answer="a",
                explanation="An operator is a custom controller that watches a CRD and reconciles the cluster state to match the desired state described in the custom resource. It encodes operational knowledge into software.",
                xp=10,
            ),
            Challenge(
                id="k8sa_reconcile_loop",
                type="fill_blank",
                prompt="In controller-runtime, the core method that handles each event is called _____",
                answer="Reconcile",
                explanation="The `Reconcile` method is the heart of every controller built with controller-runtime. It receives a Request (namespace/name of the changed object) and returns a Result that can requeue the event.",
                xp=15,
                hints=["It's a single method name", "Think: desired state vs actual state"],
            ),
            Challenge(
                id="k8sa_finalizer_purpose",
                type="quiz",
                prompt="What is the purpose of a finalizer on a Kubernetes resource?",
                options=[
                    "To prevent the resource from being modified after creation",
                    "To run cleanup logic before the resource is actually deleted",
                    "To encrypt the resource at rest in etcd",
                    "To ensure the resource is created before its dependencies",
                ],
                answer="b",
                explanation="Finalizers are keys in metadata.finalizers that block deletion until the controller removes them. This lets operators clean up external resources (databases, DNS records, cloud infra) before the Kubernetes object is garbage-collected.",
                xp=15,
            ),
            Challenge(
                id="k8sa_owner_reference",
                type="quiz",
                prompt="A Pod has an ownerReference pointing to a ReplicaSet. What happens when the ReplicaSet is deleted?",
                options=[
                    "The Pod continues running indefinitely",
                    "The Pod is garbage-collected (deleted) automatically",
                    "The Pod loses its labels but keeps running",
                    "The Pod is moved to the default namespace",
                ],
                answer="b",
                explanation="Owner references enable garbage collection. When an owner is deleted, all resources that reference it as an owner are automatically deleted by the garbage collector, unless the deletion uses the `orphan` propagation policy.",
                xp=15,
            ),
            Challenge(
                id="k8sa_operator_sdk",
                type="quiz",
                prompt="Which tool generates the scaffolding for a Go-based Kubernetes operator, including CRD types and controller boilerplate?",
                options=[
                    "helm create",
                    "kubebuilder init",
                    "kubectl create operator",
                    "kustomize build",
                ],
                answer="b",
                explanation="Kubebuilder (and the Operator SDK which wraps it) generates the project structure, API types, controller scaffolding, and Makefile targets for building and deploying a Go-based operator.",
                xp=15,
            ),
            Challenge(
                id="k8sa_crd_validation",
                type="quiz",
                prompt="How does Kubernetes validate fields on a custom resource instance before storing it?",
                options=[
                    "CRDs cannot have validation — any YAML is accepted",
                    "The controller validates fields in the Reconcile loop after storage",
                    "OpenAPI v3 schema validation defined in the CRD spec",
                    "A mandatory ValidatingWebhook must be deployed for every CRD",
                ],
                answer="c",
                explanation="CRDs support structural schemas using OpenAPI v3. The API server validates every create/update against this schema before writing to etcd. Additional validation can be added via admission webhooks, but the schema is built-in.",
                xp=15,
            ),
        ],
    ),

    # ── Zone 2: Networking Deep Dive ──────────────────────────────────
    "networking_deep_dive": Zone(
        id="networking_deep_dive",
        title="Networking Deep Dive",
        description="CNI plugins, service mesh, Cilium, eBPF, and advanced NetworkPolicy.",
        challenges=[
            Challenge(
                id="k8sa_cni_role",
                type="quiz",
                prompt="What is the role of a CNI (Container Network Interface) plugin in Kubernetes?",
                options=[
                    "It manages TLS certificates for pod-to-pod communication",
                    "It assigns IP addresses to pods and configures the network so pods can communicate",
                    "It replaces kube-proxy for service load balancing",
                    "It encrypts all traffic leaving the cluster",
                ],
                answer="b",
                explanation="A CNI plugin is responsible for allocating IP addresses to pods and setting up the networking (routes, bridges, overlays) so that every pod can reach every other pod — the fundamental Kubernetes networking requirement.",
                xp=10,
            ),
            Challenge(
                id="k8sa_cilium_ebpf",
                type="quiz",
                prompt="Cilium replaces kube-proxy by using which Linux kernel technology to handle packet routing?",
                options=[
                    "iptables",
                    "nftables",
                    "eBPF (extended Berkeley Packet Filter)",
                    "IPVS",
                ],
                answer="c",
                explanation="Cilium uses eBPF programs loaded into the Linux kernel to handle packet forwarding, load balancing, and network policy enforcement — bypassing iptables entirely for better performance and observability.",
                xp=10,
            ),
            Challenge(
                id="k8sa_service_mesh_mtls",
                type="quiz",
                prompt="In a service mesh like Istio, what does mTLS (mutual TLS) between sidecars provide?",
                options=[
                    "Authentication of both client and server pods, plus encryption of traffic in transit",
                    "Only encryption — authentication requires a separate mechanism",
                    "Rate limiting between services",
                    "Automatic retries for failed requests",
                ],
                answer="a",
                explanation="Mutual TLS means both sides present certificates. Each sidecar proxy proves its identity (authentication) AND the traffic between them is encrypted. This provides both identity verification and confidentiality at the transport layer.",
                xp=15,
            ),
            Challenge(
                id="k8sa_hubble_observe",
                type="fill_blank",
                prompt="To observe real-time network flows in a Cilium cluster, you use the CLI tool called _____",
                answer="hubble",
                explanation="Hubble is Cilium's observability layer. `hubble observe` shows real-time network flows with source/destination pod info, verdicts (forwarded/dropped), and L7 protocol details.",
                xp=15,
                hints=["Named after a famous telescope", "It's Cilium's observability component"],
            ),
            Challenge(
                id="k8sa_cilium_network_policy",
                type="quiz",
                prompt="How does a CiliumNetworkPolicy differ from a standard Kubernetes NetworkPolicy?",
                options=[
                    "CiliumNetworkPolicy is cluster-scoped, not namespaced",
                    "CiliumNetworkPolicy supports L7 rules (HTTP paths, DNS names) and identity-based filtering",
                    "CiliumNetworkPolicy only works with TCP, not UDP",
                    "There is no difference — Cilium just implements the standard spec",
                ],
                answer="b",
                explanation="CiliumNetworkPolicy extends the standard spec with L7 filtering (HTTP method/path, gRPC, Kafka topic), DNS-based rules, and Cilium identity selectors. Standard NetworkPolicy only operates at L3/L4.",
                xp=15,
            ),
            Challenge(
                id="k8sa_envoy_sidecar",
                type="quiz",
                prompt="In the Istio service mesh, what process runs as a sidecar in every meshed pod?",
                options=[
                    "nginx",
                    "haproxy",
                    "Envoy proxy",
                    "CoreDNS",
                ],
                answer="c",
                explanation="Istio injects an Envoy proxy sidecar into every pod. Envoy intercepts all inbound and outbound traffic, applying mTLS, routing rules, retries, circuit breaking, and telemetry collection.",
                xp=10,
            ),
            Challenge(
                id="k8sa_gateway_api",
                type="quiz",
                prompt="The Gateway API is replacing Ingress resources. What is the key architectural improvement?",
                options=[
                    "Gateway API only supports HTTP, making it simpler",
                    "It separates infrastructure concerns (Gateway) from application routing (HTTPRoute) with role-based ownership",
                    "It removes the need for a load balancer entirely",
                    "It merges ingress and egress into a single resource",
                ],
                answer="b",
                explanation="Gateway API introduces role-oriented design: infrastructure teams manage Gateway resources, while app teams manage HTTPRoute/TCPRoute resources. This separation of concerns and the richer API model (TCP, UDP, TLS, gRPC) is why it's replacing Ingress.",
                xp=15,
            ),
            Challenge(
                id="k8sa_dns_resolution",
                type="quiz",
                prompt="A pod in namespace `alpha` wants to reach a Service named `api` in namespace `beta`. What DNS name works?",
                options=[
                    "api.beta",
                    "api.beta.svc.cluster.local",
                    "beta.api.svc",
                    "api.svc.beta.cluster.local",
                ],
                answer="b",
                explanation="The full DNS name for a Kubernetes Service is `<service>.<namespace>.svc.cluster.local`. Cross-namespace calls must use at least `api.beta.svc` — the short name `api` only resolves within the same namespace.",
                xp=10,
            ),
        ],
    ),

    # ── Zone 3: Storage & StatefulSets ────────────────────────────────
    "storage_statefulsets": Zone(
        id="storage_statefulsets",
        title="Storage & StatefulSets",
        description="PersistentVolumes, PVCs, StorageClasses, CSI drivers, and VolumeSnapshots.",
        challenges=[
            Challenge(
                id="k8sa_storage_class",
                type="quiz",
                prompt="What does a StorageClass define in Kubernetes?",
                options=[
                    "The maximum size of all volumes in a namespace",
                    "A template for dynamically provisioning PersistentVolumes with specific parameters",
                    "The encryption algorithm used for volumes at rest",
                    "A mapping between PVCs and specific physical disks",
                ],
                answer="b",
                explanation="A StorageClass defines a 'class' of storage — the provisioner (e.g., EBS, GCE-PD), parameters (disk type, IOPS), and reclaim policy. When a PVC references a StorageClass, the provisioner dynamically creates a matching PV.",
                xp=10,
            ),
            Challenge(
                id="k8sa_csi_driver",
                type="quiz",
                prompt="What is a CSI (Container Storage Interface) driver?",
                options=[
                    "A kernel module that encrypts all disk I/O",
                    "A standardized plugin that lets Kubernetes provision, attach, and mount storage from any backend",
                    "A Kubernetes-native replacement for NFS",
                    "A container runtime interface for block devices",
                ],
                answer="b",
                explanation="CSI is a standard interface between container orchestrators and storage providers. A CSI driver runs as pods in the cluster and handles volume lifecycle operations (create, delete, attach, mount, snapshot) for its specific storage backend.",
                xp=10,
            ),
            Challenge(
                id="k8sa_pv_reclaim_policy",
                type="quiz",
                prompt="A PV has `reclaimPolicy: Retain`. The PVC bound to it is deleted. What happens to the PV and its data?",
                options=[
                    "The PV and its data are immediately deleted",
                    "The PV moves to 'Released' state — data is preserved but the PV can't be rebound without manual intervention",
                    "The PV is automatically rebound to the next PVC that matches",
                    "The data is archived to an S3-compatible object store",
                ],
                answer="b",
                explanation="With `Retain`, deleting the PVC puts the PV in `Released` state. The data stays on the underlying storage. An admin must manually remove the `claimRef`, clean up data if needed, and make the PV `Available` again for rebinding.",
                xp=15,
            ),
            Challenge(
                id="k8sa_volume_snapshot",
                type="fill_blank",
                prompt="To create a point-in-time copy of a PVC's data, you create a VolumeSnapshot referencing a _____",
                answer="VolumeSnapshotClass",
                explanation="A VolumeSnapshot references a VolumeSnapshotClass, which defines the CSI driver and parameters for snapshotting. The CSI driver then creates the snapshot on the underlying storage system.",
                xp=15,
                hints=["It's similar to how a PVC references a StorageClass", "Think: Snapshot + Class"],
            ),
            Challenge(
                id="k8sa_statefulset_ordering",
                type="quiz",
                prompt="A StatefulSet has 3 replicas. In what order are pods created during initial deployment?",
                options=[
                    "All three pods start simultaneously",
                    "pod-0 first, then pod-1, then pod-2 — each must be Running and Ready before the next starts",
                    "Random order determined by the scheduler",
                    "pod-2 first (highest ordinal), then pod-1, then pod-0",
                ],
                answer="b",
                explanation="StatefulSets create pods in ordinal order (0, 1, 2...) and wait for each to be Running and Ready before creating the next. This guarantees ordered, graceful deployment — critical for databases that need a primary to be up before replicas join.",
                xp=15,
            ),
            Challenge(
                id="k8sa_volume_mode",
                type="quiz",
                prompt="A PVC specifies `volumeMode: Block` instead of `Filesystem`. What does this mean?",
                options=[
                    "The volume is compressed using block-level deduplication",
                    "The volume is presented as a raw block device to the pod, without a filesystem",
                    "The volume can only store fixed-size blocks of data",
                    "The volume is read-only",
                ],
                answer="b",
                explanation="With `volumeMode: Block`, the volume is exposed as a raw block device (e.g., /dev/xvda) inside the pod — no filesystem is created. This is used by databases and applications that manage their own on-disk format for better performance.",
                xp=15,
            ),
            Challenge(
                id="k8sa_access_modes",
                type="quiz",
                prompt="Which PV access mode allows multiple pods on multiple nodes to read AND write to the same volume?",
                options=[
                    "ReadWriteOnce (RWO)",
                    "ReadOnlyMany (ROX)",
                    "ReadWriteMany (RWX)",
                    "ReadWriteOncePod (RWOP)",
                ],
                answer="c",
                explanation="ReadWriteMany (RWX) allows the volume to be mounted as read-write by many nodes simultaneously. This is supported by NFS, CephFS, and similar shared storage backends — but NOT by most block storage like EBS or GCE-PD.",
                xp=10,
            ),
            Challenge(
                id="k8sa_ephemeral_volume",
                type="quiz",
                prompt="What happens to a generic ephemeral volume when the pod that created it is deleted?",
                options=[
                    "The volume is retained for manual cleanup",
                    "The volume is automatically deleted because the pod is its owner",
                    "The volume is detached but persists as an unbound PV",
                    "The volume is snapshotted before deletion",
                ],
                answer="b",
                explanation="Generic ephemeral volumes set an owner reference from the PVC to the pod. When the pod is deleted, garbage collection deletes the PVC, which triggers the provisioner to delete the underlying volume. The lifecycle is tied to the pod.",
                xp=10,
            ),
        ],
    ),

    # ── Zone 4: Security & RBAC ───────────────────────────────────────
    "security_rbac": Zone(
        id="security_rbac",
        title="Security & RBAC",
        description="ServiceAccounts, Roles, ClusterRoles, Pod Security Admission, secrets encryption, and OPA.",
        challenges=[
            Challenge(
                id="k8sa_sa_token",
                type="quiz",
                prompt="In Kubernetes 1.24+, what changed about ServiceAccount tokens?",
                options=[
                    "ServiceAccounts were removed entirely",
                    "Auto-mounted tokens are now time-bound (projected volumes) instead of long-lived Secrets",
                    "Tokens are now stored in ConfigMaps instead of Secrets",
                    "Every ServiceAccount must be manually approved by an admin",
                ],
                answer="b",
                explanation="Starting in 1.24, Kubernetes no longer auto-creates long-lived Secret-based tokens. Instead, the TokenRequest API issues time-bound, audience-scoped tokens projected into pods. This reduces the blast radius of token theft.",
                xp=10,
            ),
            Challenge(
                id="k8sa_role_vs_clusterrole",
                type="quiz",
                prompt="What is the key difference between a Role and a ClusterRole?",
                options=[
                    "A Role grants more permissions than a ClusterRole",
                    "A Role is namespaced; a ClusterRole is cluster-scoped and can grant access to cluster-wide resources",
                    "A ClusterRole can only be used by cluster administrators",
                    "Roles use allow rules; ClusterRoles use deny rules",
                ],
                answer="b",
                explanation="A Role grants permissions within a single namespace. A ClusterRole can grant the same namespaced permissions (when bound with a RoleBinding) OR access to cluster-scoped resources like Nodes and PersistentVolumes (when bound with a ClusterRoleBinding).",
                xp=10,
            ),
            Challenge(
                id="k8sa_escalate_verb",
                type="quiz",
                prompt="The RBAC verb `escalate` on Roles/ClusterRoles is dangerous. What does it allow?",
                options=[
                    "Running pods with elevated Linux capabilities",
                    "Creating or modifying Roles/ClusterRoles with more permissions than the user currently has",
                    "Bypassing admission webhooks for a single request",
                    "Accessing the etcd database directly",
                ],
                answer="b",
                explanation="The `escalate` verb lets a user create or update Roles with permissions they don't hold. Without it, Kubernetes blocks privilege escalation by default — you can't grant what you don't have. This verb bypasses that check.",
                xp=15,
            ),
            Challenge(
                id="k8sa_psa_enforce",
                type="quiz",
                prompt="Pod Security Admission (PSA) replaced PodSecurityPolicies. What are the three PSA enforcement levels?",
                options=[
                    "allow, deny, audit",
                    "privileged, baseline, restricted",
                    "low, medium, high",
                    "permissive, standard, strict",
                ],
                answer="b",
                explanation="PSA defines three security profiles: `privileged` (unrestricted), `baseline` (prevents known privilege escalations), and `restricted` (hardened best practices). Each namespace can set `enforce`, `audit`, or `warn` modes per profile.",
                xp=15,
            ),
            Challenge(
                id="k8sa_secrets_encryption",
                type="fill_blank",
                prompt="To encrypt Secrets at rest in etcd, you configure an EncryptionConfiguration with a provider such as _____",
                answer="aescbc",
                explanation="The EncryptionConfiguration file specifies providers like `aescbc`, `aesgcm`, `secretbox`, or `kms` (for external key management). Without this, Secrets are stored as base64-encoded plaintext in etcd — readable by anyone with etcd access.",
                xp=15,
                hints=["It's a block cipher mode", "AES + a mode of operation"],
            ),
            Challenge(
                id="k8sa_opa_gatekeeper",
                type="quiz",
                prompt="What is OPA Gatekeeper's role in a Kubernetes cluster?",
                options=[
                    "It replaces RBAC for all authorization decisions",
                    "It acts as a validating admission webhook that enforces custom policies written in Rego",
                    "It manages TLS certificates for all cluster services",
                    "It provides a web UI for cluster administration",
                ],
                answer="b",
                explanation="Gatekeeper is an admission controller that uses OPA (Open Policy Agent) to evaluate custom policies written in Rego. It can block non-compliant resources at admission time — e.g., requiring labels, blocking privileged pods, enforcing image registries.",
                xp=15,
            ),
            Challenge(
                id="k8sa_audit_logging",
                type="quiz",
                prompt="Kubernetes audit logging captures API server requests. Which audit level records the request body but NOT the response body?",
                options=[
                    "None",
                    "Metadata",
                    "Request",
                    "RequestResponse",
                ],
                answer="c",
                explanation="The four audit levels are: `None` (don't log), `Metadata` (log request metadata only), `Request` (log metadata + request body), `RequestResponse` (log metadata + request body + response body). `Request` level captures what was submitted without the overhead of response bodies.",
                xp=10,
            ),
            Challenge(
                id="k8sa_impersonation",
                type="quiz",
                prompt="The `--as` flag in kubectl uses which Kubernetes feature that allows acting as another user or ServiceAccount?",
                options=[
                    "Token exchange",
                    "User impersonation",
                    "Proxy authentication",
                    "Identity federation",
                ],
                answer="b",
                explanation="User impersonation lets an authenticated user act as another user, group, or ServiceAccount. It requires the `impersonate` verb in RBAC. `kubectl --as=system:serviceaccount:ns:sa` is invaluable for testing RBAC policies without switching kubeconfigs.",
                xp=10,
            ),
        ],
    ),

    # ── Zone 5: Cluster Operations ────────────────────────────────────
    "cluster_operations": Zone(
        id="cluster_operations",
        title="Cluster Operations",
        description="Cluster upgrades, etcd backup/restore, node management, and cost optimization.",
        challenges=[
            Challenge(
                id="k8sa_upgrade_order",
                type="quiz",
                prompt="When upgrading a Kubernetes cluster, what is the correct order of component upgrades?",
                options=[
                    "Workers first, then control plane, then etcd",
                    "etcd first, then control plane, then workers",
                    "Control plane (API server, controller-manager, scheduler) first, then workers",
                    "All components must be upgraded simultaneously",
                ],
                answer="c",
                explanation="The control plane must be upgraded first because kubelets (workers) can be up to 2 minor versions behind the API server, but not the reverse. Upgrade sequence: control plane components → then worker nodes one at a time.",
                xp=10,
            ),
            Challenge(
                id="k8sa_version_skew",
                type="quiz",
                prompt="Kubernetes supports a version skew of how many minor versions between the API server and kubelets?",
                options=[
                    "0 — they must match exactly",
                    "1 minor version",
                    "2 minor versions (kubelet can be up to n-2)",
                    "3 minor versions",
                ],
                answer="c",
                explanation="Kubelets can be up to 2 minor versions older than the API server (e.g., API server v1.29, kubelet v1.27). This allows rolling upgrades of large clusters. But kubelets must never be NEWER than the API server.",
                xp=10,
            ),
            Challenge(
                id="k8sa_etcd_snapshot",
                type="fill_blank",
                prompt="To take an etcd backup, you run: etcdctl _____ save /backup/etcd-snapshot.db",
                answer="snapshot",
                explanation="`etcdctl snapshot save` creates a point-in-time backup of the entire etcd database. This captures all cluster state: pods, services, secrets, RBAC rules — everything. Regular snapshots are critical for disaster recovery.",
                xp=15,
                hints=["One word — a point-in-time copy", "Think: photography"],
            ),
            Challenge(
                id="k8sa_cordon_drain",
                type="quiz",
                prompt="What is the difference between `kubectl cordon` and `kubectl drain`?",
                options=[
                    "They are aliases for the same operation",
                    "`cordon` marks a node as unschedulable; `drain` also evicts all existing pods from the node",
                    "`cordon` stops the kubelet; `drain` stops the container runtime",
                    "`cordon` is for control-plane nodes; `drain` is for workers",
                ],
                answer="b",
                explanation="`cordon` sets the node to `SchedulingDisabled` — no new pods will be placed there. `drain` does the same AND evicts all pods (respecting PodDisruptionBudgets and grace periods). Use `drain` before maintenance or upgrades.",
                xp=15,
            ),
            Challenge(
                id="k8sa_pdb_purpose",
                type="quiz",
                prompt="A PodDisruptionBudget (PDB) with `minAvailable: 2` protects a Deployment with 3 replicas. What happens during a drain?",
                options=[
                    "All 3 pods are evicted immediately",
                    "Only 1 pod can be evicted at a time — the drain ensures at least 2 remain running",
                    "The drain is blocked entirely until the PDB is deleted",
                    "The PDB is ignored during drain operations",
                ],
                answer="b",
                explanation="PDBs limit voluntary disruptions. With `minAvailable: 2` and 3 replicas, only 1 pod can be evicted at a time. The drain will evict one, wait for a replacement to be ready, then evict the next. This prevents service outages during maintenance.",
                xp=15,
            ),
            Challenge(
                id="k8sa_taint_toleration",
                type="quiz",
                prompt="A node has the taint `gpu=true:NoSchedule`. Which pods can be scheduled on it?",
                options=[
                    "Any pod — taints are only advisory",
                    "Only pods with a matching toleration for `gpu=true`",
                    "Only pods in the `kube-system` namespace",
                    "Only DaemonSet pods",
                ],
                answer="b",
                explanation="Taints repel pods unless they have a matching toleration. `NoSchedule` means no pod will be scheduled on the node unless it tolerates the taint. This is used to reserve nodes for specific workloads (GPU, high-memory) or to prevent scheduling during maintenance.",
                xp=10,
            ),
            Challenge(
                id="k8sa_node_affinity",
                type="quiz",
                prompt="What is the difference between `requiredDuringSchedulingIgnoredDuringExecution` and `preferredDuringSchedulingIgnoredDuringExecution` in node affinity?",
                options=[
                    "Required is for DaemonSets; Preferred is for Deployments",
                    "Required is a hard constraint (pod won't schedule if no match); Preferred is a soft hint (scheduler tries but can place elsewhere)",
                    "Required applies to CPU; Preferred applies to memory",
                    "They are identical — the names are just different API versions",
                ],
                answer="b",
                explanation="'Required' is a hard rule — the scheduler will NOT place the pod if no node matches. 'Preferred' is a soft rule — the scheduler tries to match but will schedule on other nodes if needed. Both are 'IgnoredDuringExecution' meaning running pods aren't evicted if labels change.",
                xp=15,
            ),
            Challenge(
                id="k8sa_cost_optimization",
                type="quiz",
                prompt="Which Kubernetes feature automatically adjusts pod resource requests based on actual usage over time?",
                options=[
                    "HorizontalPodAutoscaler (HPA)",
                    "VerticalPodAutoscaler (VPA)",
                    "LimitRange",
                    "ResourceQuota",
                ],
                answer="b",
                explanation="The VPA monitors actual CPU and memory usage and recommends (or automatically applies) adjusted resource requests. This prevents over-provisioning (wasted money) and under-provisioning (OOM kills). HPA scales replicas; VPA scales the pod's resource allocation.",
                xp=10,
            ),
        ],
    ),
}
