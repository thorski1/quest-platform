"""
zones.py — DevSecOps skill pack zones for NEXUS Quest.
5 zones, 40 challenges. The pipeline is the perimeter.
"""

from engine.zone import Zone
from engine.challenge import Challenge

ZONE_ORDER = [
    "shift_left_security",
    "secret_management",
    "supply_chain_security",
    "runtime_security",
    "compliance_as_code",
]

ZONES = {
    # ──────────────────────────────────────────────
    # ZONE 1 — SHIFT LEFT SECURITY
    # ──────────────────────────────────────────────
    "shift_left_security": Zone(
        id="shift_left_security",
        title="Shift Left Security",
        description="SAST, DAST, and SCA integrated into CI — catch vulnerabilities before they merge.",
        challenges=[
            Challenge(
                id="sls_sast_definition",
                type="quiz",
                prompt="What does SAST stand for, and when does it run in a secure pipeline?",
                options=[
                    "System Administration Security Testing — runs in production",
                    "Static Application Security Testing — analyzes source code without executing it, typically in CI before merge",
                    "Secure Access Service Tunnel — encrypts traffic between services",
                    "Software Artifact Signing Tool — signs build outputs",
                ],
                answer="b",
                explanation="SAST (Static Application Security Testing) scans source code, bytecode, or binaries for security vulnerabilities without executing the program. It runs early in CI — ideally on every pull request — to catch issues like SQL injection, XSS, and hardcoded secrets before code merges. Tools include Semgrep, SonarQube, and CodeQL.",
                hints=["The 'S' in SAST stands for Static — no execution needed.", "It analyzes the code itself, not the running application."],
                xp=25,
            ),
            Challenge(
                id="sls_dast_vs_sast",
                type="quiz",
                prompt="How does DAST differ from SAST?",
                options=[
                    "DAST analyzes source code; SAST tests running applications",
                    "DAST tests the running application from the outside like an attacker; SAST analyzes code without executing it",
                    "DAST is faster because it skips compilation",
                    "DAST only works with compiled languages; SAST only works with interpreted languages",
                ],
                answer="b",
                explanation="DAST (Dynamic Application Security Testing) interacts with a running application — sending malicious inputs, fuzzing endpoints, testing authentication flows. It finds runtime vulnerabilities SAST can't see: misconfigurations, authentication flaws, server-side issues. Tools include OWASP ZAP, Burp Suite, and Nuclei. SAST and DAST are complementary, not competing.",
                hints=["Dynamic means it needs the app to be running.", "Think: SAST reads the code, DAST attacks the app."],
                xp=25,
            ),
            Challenge(
                id="sls_sca_purpose",
                type="quiz",
                prompt="What is the primary purpose of Software Composition Analysis (SCA)?",
                options=[
                    "To analyze code quality metrics like cyclomatic complexity",
                    "To identify known vulnerabilities in open-source dependencies and libraries",
                    "To compose microservices into a deployment manifest",
                    "To perform load testing on composed service architectures",
                ],
                answer="b",
                explanation="SCA tools (Snyk, Dependabot, Trivy, Grype) scan your dependency tree against vulnerability databases like the NVD and OSV. They flag known CVEs in libraries you depend on — including transitive dependencies you never explicitly chose. SCA is critical because most application code is third-party code.",
                hints=["It's about analyzing what your software is composed of.", "Think: your dependencies are your attack surface."],
                xp=25,
            ),
            Challenge(
                id="sls_semgrep_role",
                type="quiz",
                prompt="Semgrep is a popular SAST tool. What makes it particularly effective for DevSecOps pipelines?",
                options=[
                    "It requires a full compilation step before scanning",
                    "It uses lightweight, pattern-based rules that run fast, support custom rules, and integrate easily into CI",
                    "It only works with Java applications",
                    "It replaces the need for DAST entirely",
                ],
                answer="b",
                explanation="Semgrep uses pattern-matching rules written in a simple YAML syntax. It's fast (no compilation needed), supports 30+ languages, and lets teams write custom rules for their own anti-patterns. It runs in seconds in CI, making it practical as a merge gate without slowing developers down.",
                hints=["Think about developer experience — speed and customization matter.", "It's pattern-based, not compiler-based."],
                xp=30,
            ),
            Challenge(
                id="sls_fail_open",
                type="quiz",
                prompt="A SAST tool is configured in CI but set to 'warn only' — it reports findings but never blocks the merge. What is this anti-pattern called?",
                options=[
                    "Fail-open — security checks run but don't enforce, so findings are ignored",
                    "Defense in depth — multiple layers allow flexibility",
                    "Shift-right — security testing deferred to production",
                    "Blue team passive — security observes but doesn't intervene",
                ],
                answer="a",
                explanation="Fail-open security means the controls exist but don't enforce. Developers learn to ignore warnings that never block their work. Effective DevSecOps requires fail-closed gates: critical and high findings block the merge. Medium findings can warn. The key is tuning severity thresholds so the gate is meaningful, not noisy.",
                hints=["If the check never stops anything, does it really protect?", "Opposite of fail-closed."],
                xp=30,
            ),
            Challenge(
                id="sls_pre_commit_hooks",
                type="text",
                prompt="Name a security tool or check you would run as a pre-commit hook (before code even reaches CI) to prevent secrets from being committed to version control.",
                options=[],
                answer="gitleaks",
                explanation="Tools like gitleaks, detect-secrets, and truffleHog can run as pre-commit hooks to scan staged changes for API keys, passwords, tokens, and private keys before they enter git history. Once a secret is committed, it lives in git history forever (even after deletion) unless you rewrite history — which is painful and disruptive.",
                hints=["Think about scanning for API keys and passwords before they hit git.", "The tool name references 'leaks' in git."],
                xp=30,
            ),
            Challenge(
                id="sls_trivy_scan",
                type="quiz",
                prompt="In a CI pipeline, at which stage should you run a container image vulnerability scan with a tool like Trivy?",
                options=[
                    "Only in production after deployment",
                    "After the image is built but before it is pushed to the registry",
                    "Only during the annual security audit",
                    "Before writing the Dockerfile",
                ],
                answer="b",
                explanation="Scan the image after build but before push. This catches OS-level CVEs, library vulnerabilities, and misconfigurations before the image enters your registry. If the scan finds critical vulnerabilities, the pipeline fails and the image never ships. Trivy, Grype, and Snyk Container are popular choices.",
                hints=["You need the built image to scan it, but you don't want vulnerable images in the registry.", "Think: build -> scan -> push."],
                xp=30,
            ),
            Challenge(
                id="sls_pipeline_ordering",
                type="text",
                prompt="In a secure CI pipeline, what is the recommended order of security checks? List the three types: SAST, SCA, DAST.",
                options=[],
                answer="SAST, SCA, DAST",
                explanation="SAST runs first (fastest, no deployment needed — scans source code). SCA runs next (checks dependency manifests against vulnerability databases). DAST runs last (requires a running application, slowest). This ordering follows the principle of fast feedback: catch the easiest issues first, defer expensive checks to later stages.",
                hints=["Order by speed and what's needed: code -> dependencies -> running app.", "Static checks are fastest; dynamic checks need a deployed app."],
                xp=35,
            ),
        ],
    ),

    # ──────────────────────────────────────────────
    # ZONE 2 — SECRET MANAGEMENT
    # ──────────────────────────────────────────────
    "secret_management": Zone(
        id="secret_management",
        title="The Vault",
        description="Centralized secret storage, sealed secrets, rotation, and runtime injection — no more hardcoded credentials.",
        challenges=[
            Challenge(
                id="sm_why_not_env",
                type="quiz",
                prompt="Why are plain environment variables a poor choice for storing secrets in production?",
                options=[
                    "Environment variables are too slow to read",
                    "They can leak into logs, crash dumps, child processes, and are visible to anyone with shell access to the container",
                    "Environment variables don't support strings longer than 256 characters",
                    "They are encrypted by default so you can't read them",
                ],
                answer="b",
                explanation="Environment variables leak in many ways: process listings (ps auxe), debug endpoints, crash dumps, Docker inspect, child process inheritance, and CI build logs. They have no access control, no audit trail, and no rotation mechanism. Centralized secret managers (Vault, AWS Secrets Manager) solve all of these problems.",
                hints=["Think about all the places env vars can be seen unintentionally.", "ps, logs, debug pages, inspect commands..."],
                xp=25,
            ),
            Challenge(
                id="sm_vault_basics",
                type="quiz",
                prompt="HashiCorp Vault provides dynamic secrets. What does 'dynamic secret' mean?",
                options=[
                    "A secret that changes its encryption algorithm automatically",
                    "A short-lived credential generated on-demand for a specific client, with automatic expiration",
                    "A password stored in a dynamic programming language",
                    "A secret that is only available during business hours",
                ],
                answer="b",
                explanation="Dynamic secrets are generated on-demand when a client requests them. Vault can create a unique database credential for each application instance with a TTL (e.g., 1 hour). When the TTL expires, Vault revokes the credential. If compromised, the blast radius is one credential that expires soon — not a shared password that lives forever.",
                hints=["Dynamic means generated on the fly, not stored permanently.", "Think: unique, short-lived, auto-revoking."],
                xp=30,
            ),
            Challenge(
                id="sm_sealed_secrets",
                type="quiz",
                prompt="In Kubernetes, SealedSecrets (Bitnami) solve which specific problem?",
                options=[
                    "Encrypting network traffic between pods",
                    "Safely storing encrypted secrets in Git so they can be managed with GitOps workflows",
                    "Sealing container images to prevent tampering",
                    "Replacing TLS certificates with symmetric encryption",
                ],
                answer="b",
                explanation="SealedSecrets let you encrypt Kubernetes Secret manifests with a public key. The encrypted SealedSecret resource can be safely committed to Git. Only the SealedSecret controller in the cluster (which holds the private key) can decrypt it back into a regular Secret. This enables GitOps for secrets without exposing plaintext values in the repository.",
                hints=["The problem is: how do you store secrets in Git safely?", "GitOps needs everything in Git — including secrets."],
                xp=30,
            ),
            Challenge(
                id="sm_rotation_why",
                type="quiz",
                prompt="Why is automatic secret rotation critical even when there's no known compromise?",
                options=[
                    "Rotated secrets use less memory",
                    "It limits the window of exposure if a secret is compromised without your knowledge, and ensures stale credentials can't be used indefinitely",
                    "Compliance requires rotation every 24 hours",
                    "Rotation improves application performance",
                ],
                answer="b",
                explanation="You can't always know when a secret is compromised. Automatic rotation ensures that even if a credential leaks, it becomes useless after the rotation interval. It also enforces good hygiene: no eternal credentials, no forgotten service account passwords. Most compliance frameworks (SOC 2, PCI-DSS) require rotation policies.",
                hints=["Assume breach — how do you limit damage from an unknown compromise?", "If a secret was stolen last month and you don't know, rotation invalidates it."],
                xp=30,
            ),
            Challenge(
                id="sm_external_secrets_operator",
                type="text",
                prompt="Name the Kubernetes operator that syncs secrets from external providers (AWS Secrets Manager, Vault, GCP Secret Manager) into Kubernetes Secret objects.",
                options=[],
                answer="External Secrets Operator",
                explanation="The External Secrets Operator (ESO) watches ExternalSecret custom resources and syncs secret data from external providers into native Kubernetes Secrets. This means your applications consume standard K8s Secrets, but the source of truth lives in a centralized secret manager with proper access control, rotation, and audit logging.",
                hints=["The name literally describes what it does: external secrets.", "It's a Kubernetes operator — watches CRDs and reconciles state."],
                xp=30,
            ),
            Challenge(
                id="sm_vault_auth_methods",
                type="quiz",
                prompt="Which Vault authentication method is most appropriate for a Kubernetes workload?",
                options=[
                    "Username/password stored in a ConfigMap",
                    "Kubernetes auth method — Vault validates the pod's service account token with the Kubernetes API",
                    "SSH key-based authentication",
                    "LDAP authentication with a shared service account",
                ],
                answer="b",
                explanation="Vault's Kubernetes auth method validates a pod's service account JWT with the Kubernetes API server. No passwords or tokens need to be distributed — the pod's identity IS the credential. Vault policies then map service accounts to specific secret paths, ensuring each workload can only access its own secrets.",
                hints=["The pod already has an identity — its service account.", "No extra credentials needed if Vault trusts the K8s API."],
                xp=35,
            ),
            Challenge(
                id="sm_secret_zero",
                type="quiz",
                prompt="The 'secret zero' problem refers to:",
                options=[
                    "The first secret ever created in a system",
                    "The challenge of securely delivering the initial credential that an application uses to authenticate to a secret manager",
                    "A secret with a value of zero bytes",
                    "The root token of HashiCorp Vault",
                ],
                answer="b",
                explanation="If your app needs to authenticate to Vault to get secrets, what authenticates the app to Vault? That initial credential is 'secret zero.' Solutions include: Kubernetes service account tokens (identity-based, no secret to distribute), cloud IAM roles (instance metadata), AppRole with secure introduction, and Vault Agent with auto-auth.",
                hints=["It's a chicken-and-egg problem.", "How do you authenticate to the thing that stores your authentication?"],
                xp=35,
            ),
            Challenge(
                id="sm_least_privilege_secrets",
                type="text",
                prompt="In Vault, what mechanism restricts which secrets a given application can access? (Name the concept.)",
                options=[],
                answer="policies",
                explanation="Vault policies are written in HCL and define which secret paths a token or identity can read, write, list, or delete. Following least privilege, each application gets a policy granting access only to its own secret paths. A payment service can read 'secret/data/payment/*' but not 'secret/data/user-service/*'. Policies are the authorization layer.",
                hints=["It's Vault's authorization mechanism.", "Written in HCL, attached to tokens and auth methods."],
                xp=35,
            ),
        ],
    ),

    # ──────────────────────────────────────────────
    # ZONE 3 — SUPPLY CHAIN SECURITY
    # ──────────────────────────────────────────────
    "supply_chain_security": Zone(
        id="supply_chain_security",
        title="The Chain of Trust",
        description="SBOMs, Sigstore, SLSA provenance, and admission control — verify every artifact from source to deploy.",
        challenges=[
            Challenge(
                id="scs_sbom_definition",
                type="quiz",
                prompt="What is an SBOM and why is it critical for supply chain security?",
                options=[
                    "Software Build Operations Manual — documents build procedures",
                    "Software Bill of Materials — a complete inventory of all components, libraries, and dependencies in a software artifact",
                    "Secure Binary Object Map — a memory layout for secure applications",
                    "System Backup and Operations Manifest — a disaster recovery document",
                ],
                answer="b",
                explanation="An SBOM is a machine-readable inventory of every component in your software — direct dependencies, transitive dependencies, OS packages, and their versions. When a new CVE drops (like Log4Shell), an SBOM lets you instantly answer: 'Are we affected?' Formats include SPDX and CycloneDX. US Executive Order 14028 mandates SBOMs for government software.",
                hints=["Think: ingredient list for software.", "The 'BOM' part comes from manufacturing — Bill of Materials."],
                xp=25,
            ),
            Challenge(
                id="scs_sigstore_cosign",
                type="quiz",
                prompt="Sigstore's cosign tool is used to:",
                options=[
                    "Co-sign legal documents between software vendors",
                    "Sign and verify container images using keyless or key-based cryptographic signatures",
                    "Synchronize container registries across regions",
                    "Compress container image layers for faster pulls",
                ],
                answer="b",
                explanation="cosign signs container images with cryptographic signatures. In keyless mode, it uses OIDC identity (your GitHub/Google identity) and records signatures in Rekor, a tamper-proof transparency log. Consumers verify that the image was built by a trusted identity and hasn't been tampered with. This is the foundation of software supply chain trust.",
                hints=["It's about cryptographic signing of containers.", "Keyless mode uses your existing identity provider."],
                xp=30,
            ),
            Challenge(
                id="scs_slsa_levels",
                type="quiz",
                prompt="SLSA (Supply-chain Levels for Software Artifacts) defines build integrity levels. What does SLSA Level 3 require?",
                options=[
                    "That the source code is written in a memory-safe language",
                    "That the build process is fully defined in source, runs on an ephemeral isolated environment, and produces verifiable provenance",
                    "That all developers have security certifications",
                    "That the application passes a penetration test",
                ],
                answer="b",
                explanation="SLSA L3 requires: build defined as code (reproducible), build runs on an isolated ephemeral environment (not a developer laptop), and the build system generates non-falsifiable provenance (cryptographic attestation of what was built, from what source, by what builder). This prevents tampering at every stage from source to artifact.",
                hints=["SLSA is about build integrity, not code quality.", "Think: can you prove the artifact matches the source?"],
                xp=35,
            ),
            Challenge(
                id="scs_rekor_transparency",
                type="text",
                prompt="In the Sigstore ecosystem, what is Rekor?",
                options=[],
                answer="transparency log",
                explanation="Rekor is an immutable, tamper-proof transparency log that records signing events. When cosign signs an image, the signature is recorded in Rekor with a timestamp. Anyone can verify that a signature existed at a specific time and hasn't been revoked. It's modeled after Certificate Transparency logs used for TLS certificates.",
                hints=["It records signing events in an append-only log.", "Similar to Certificate Transparency, but for software artifacts."],
                xp=30,
            ),
            Challenge(
                id="scs_admission_controller",
                type="quiz",
                prompt="A Kubernetes admission controller like Kyverno or OPA Gatekeeper can enforce supply chain security by:",
                options=[
                    "Compiling source code inside the cluster",
                    "Rejecting pod deployments that reference unsigned container images or images without valid provenance attestations",
                    "Automatically patching vulnerable containers at runtime",
                    "Replacing all container images with hardened base images",
                ],
                answer="b",
                explanation="Admission controllers intercept API requests before objects are persisted. A policy can require that every container image has a valid cosign signature from a trusted key, an SBOM attestation, and SLSA provenance. If any check fails, the deployment is rejected. This is the enforcement point — the gate between your supply chain and your runtime.",
                hints=["Admission controllers are gatekeepers for the K8s API.", "They can validate image signatures before allowing deployment."],
                xp=30,
            ),
            Challenge(
                id="scs_typosquatting",
                type="quiz",
                prompt="A developer installs 'requets' instead of 'requests' from PyPI. This supply chain attack is called:",
                options=[
                    "Dependency confusion — mixing internal and public packages",
                    "Typosquatting — publishing malicious packages with names similar to popular ones",
                    "Protestware — maintainer sabotaging their own package",
                    "Cryptojacking — hijacking compute for mining",
                ],
                answer="b",
                explanation="Typosquatting exploits human error: an attacker publishes a malicious package with a name one character off from a popular package. When a developer makes a typo, they install the attacker's code. Defenses: lock files with integrity hashes, allow-listed registries, SCA tools that flag unknown packages, and private registry mirrors.",
                hints=["The name is a 'typo' of the real package name.", "Think: squatting on a misspelled domain, but for packages."],
                xp=25,
            ),
            Challenge(
                id="scs_dependency_confusion",
                type="quiz",
                prompt="Dependency confusion attacks exploit:",
                options=[
                    "The difference between HTTP and HTTPS package downloads",
                    "Package managers that check public registries before (or instead of) private registries, allowing an attacker to publish a higher-version package publicly",
                    "Weak encryption in package signing",
                    "Misconfigured DNS records for package registries",
                ],
                answer="b",
                explanation="If your project uses a private package 'corp-utils@1.0.0' and an attacker publishes 'corp-utils@99.0.0' on the public registry, many package managers will prefer the higher version from the public source. Defenses: scoped packages (@corp/utils), registry allow-lists, lock files, and configuring package managers to never fall through to public for internal names.",
                hints=["It's about private vs. public registries and version resolution.", "Higher version numbers on public registries can hijack installs."],
                xp=35,
            ),
            Challenge(
                id="scs_lockfile_importance",
                type="text",
                prompt="Why should lock files (package-lock.json, poetry.lock, go.sum) be committed to version control? Name the supply chain security reason.",
                options=[],
                answer="integrity",
                explanation="Lock files pin exact dependency versions and include cryptographic hashes of each package. This ensures every build uses identical dependencies — no surprise upgrades, no substitutions. If a dependency is compromised after you lock it, your builds are unaffected until you explicitly update. Without lock files, builds are non-reproducible and vulnerable to supply chain substitution.",
                hints=["Lock files ensure builds are reproducible.", "They contain hashes that verify package integrity."],
                xp=30,
            ),
        ],
    ),

    # ──────────────────────────────────────────────
    # ZONE 4 — RUNTIME SECURITY
    # ──────────────────────────────────────────────
    "runtime_security": Zone(
        id="runtime_security",
        title="The Watchtower",
        description="Falco, eBPF, network policies, and runtime threat detection — the last line of defense.",
        challenges=[
            Challenge(
                id="rs_falco_purpose",
                type="quiz",
                prompt="What is Falco and what does it detect?",
                options=[
                    "A container orchestrator that replaces Kubernetes",
                    "A runtime security tool that detects anomalous system calls and behaviors in containers and hosts using kernel-level instrumentation",
                    "A static code analysis tool for Go",
                    "A network firewall for Kubernetes services",
                ],
                answer="b",
                explanation="Falco (CNCF project) monitors system calls in real-time using eBPF or a kernel module. It detects anomalous behavior: shells spawned in containers, unexpected network connections, sensitive file reads, privilege escalations. Rules are written in YAML. Falco is your runtime threat detection — it sees what your application is actually doing, not what it's supposed to do.",
                hints=["It watches syscalls at the kernel level.", "Named after a bird of prey — it watches from above."],
                xp=25,
            ),
            Challenge(
                id="rs_ebpf_explained",
                type="quiz",
                prompt="eBPF (extended Berkeley Packet Filter) is used in runtime security because:",
                options=[
                    "It replaces the Linux kernel with a more secure version",
                    "It allows running sandboxed programs in the Linux kernel without modifying kernel source or loading kernel modules, enabling deep observability",
                    "It encrypts all network packets at the kernel level",
                    "It's a container runtime that replaces containerd",
                ],
                answer="b",
                explanation="eBPF lets you attach programs to kernel events (syscalls, network packets, file access) without kernel patches or modules. It's sandboxed (verified before execution) and efficient. Security tools like Falco, Tetragon, and Cilium use eBPF to observe and enforce security policies at the kernel level — faster and safer than userspace agents.",
                hints=["It runs inside the kernel but is safely sandboxed.", "Think: programmable kernel hooks without kernel development."],
                xp=30,
            ),
            Challenge(
                id="rs_network_policy_default",
                type="quiz",
                prompt="By default, Kubernetes network policies are:",
                options=[
                    "Allow-all — all pods can communicate with all other pods unless a NetworkPolicy restricts it",
                    "Deny-all — no pods can communicate until explicitly allowed",
                    "Namespace-isolated — pods can only talk within their namespace",
                    "Encrypted — all pod-to-pod traffic uses mTLS",
                ],
                answer="a",
                explanation="By default, Kubernetes has no network restrictions — every pod can reach every other pod and any external endpoint. NetworkPolicies are additive deny rules: once you create a policy selecting a pod, unmatched traffic is denied. Best practice: start with a default-deny policy per namespace, then explicitly allow required communication paths. This is zero-trust networking.",
                hints=["Kubernetes is permissive by default.", "No NetworkPolicy = no restrictions."],
                xp=25,
            ),
            Challenge(
                id="rs_default_deny_policy",
                type="text",
                prompt="What is the first NetworkPolicy you should apply to a Kubernetes namespace to establish zero-trust networking? Describe the policy in a few words.",
                options=[],
                answer="default deny all",
                explanation="A default-deny-all NetworkPolicy selects all pods in the namespace (podSelector: {}) and specifies empty ingress and egress rules. This blocks all traffic by default. You then create explicit allow policies for each legitimate communication path. This flips the model from 'everything allowed unless blocked' to 'everything blocked unless allowed.'",
                hints=["Start by blocking everything.", "podSelector: {} with no ingress/egress rules."],
                xp=30,
            ),
            Challenge(
                id="rs_falco_rule_example",
                type="quiz",
                prompt="Which Falco rule would detect an attacker running a shell inside a production container?",
                options=[
                    "A rule that monitors CPU usage above 90%",
                    "A rule that triggers when a shell binary (bash, sh, zsh) is spawned as a child process inside a container",
                    "A rule that checks if the container image is from an approved registry",
                    "A rule that monitors HTTP response codes for 500 errors",
                ],
                answer="b",
                explanation="Falco's built-in rule 'Terminal shell in container' triggers when a shell process (bash, sh, csh, zsh) is executed inside a container. In production, containers should run their designated process — a shell spawning usually indicates an attacker or unauthorized debugging. The rule watches the execve syscall for shell binary names.",
                hints=["Shells shouldn't be running in production containers.", "Falco watches process execution syscalls."],
                xp=30,
            ),
            Challenge(
                id="rs_tetragon_vs_falco",
                type="quiz",
                prompt="Cilium Tetragon extends runtime security beyond detection. What capability does it add that Falco alone does not provide?",
                options=[
                    "Tetragon uses eBPF for monitoring (Falco uses something different)",
                    "Tetragon can enforce security policies by killing processes or blocking syscalls in real-time, not just alerting",
                    "Tetragon is the only tool that can monitor network traffic",
                    "Tetragon replaces Kubernetes RBAC",
                ],
                answer="b",
                explanation="While Falco detects and alerts, Tetragon can enforce — it can terminate a process, block a syscall, or kill a pod in real-time when a policy is violated. This is runtime enforcement, not just detection. Tetragon uses eBPF TracingPolicies to define allowed behaviors and automatically prevent violations at the kernel level.",
                hints=["The key difference is enforcement vs. detection.", "Tetragon can take action, not just alert."],
                xp=35,
            ),
            Challenge(
                id="rs_pod_security_standards",
                type="quiz",
                prompt="Kubernetes Pod Security Standards define three levels. Which level should production workloads use?",
                options=[
                    "Privileged — no restrictions for maximum flexibility",
                    "Baseline — prevents known privilege escalations",
                    "Restricted — heavily restricted, following hardening best practices (no root, no privilege escalation, read-only root filesystem)",
                    "Custom — each team defines their own standards",
                ],
                answer="c",
                explanation="The Restricted level enforces: non-root user, no privilege escalation, no hostPath mounts, no host networking, restricted volume types, required seccomp profile, and dropped capabilities. It follows the principle of least privilege at the pod level. Baseline is the minimum; Restricted is the target for production. Privileged should only be used for system-level components.",
                hints=["Production should be locked down, not permissive.", "The most restrictive standard follows hardening best practices."],
                xp=30,
            ),
            Challenge(
                id="rs_runtime_immutability",
                type="text",
                prompt="Setting a container's root filesystem to read-only prevents a common class of runtime attacks. What Kubernetes securityContext field enables this?",
                options=[],
                answer="readOnlyRootFilesystem",
                explanation="Setting readOnlyRootFilesystem: true in the container's securityContext makes the root filesystem immutable. An attacker who compromises the application can't write malicious binaries, modify configurations, or install tools. If the app needs to write temporary files, mount an emptyDir volume at the specific path. This drastically reduces the attack surface of a running container.",
                hints=["It's a boolean field in securityContext.", "It makes the root filesystem read-only."],
                xp=35,
            ),
        ],
    ),

    # ──────────────────────────────────────────────
    # ZONE 5 — COMPLIANCE AS CODE
    # ──────────────────────────────────────────────
    "compliance_as_code": Zone(
        id="compliance_as_code",
        title="The Audit Trail",
        description="CIS benchmarks, SOC 2, HIPAA automation — continuous compliance, not quarterly archaeology.",
        challenges=[
            Challenge(
                id="cac_what_is_cac",
                type="quiz",
                prompt="What does 'compliance as code' mean?",
                options=[
                    "Writing compliance reports in a programming language",
                    "Expressing compliance requirements as automated, testable policies that run continuously in CI/CD pipelines",
                    "Hiring developers to work in the compliance department",
                    "Using AI to generate compliance documentation",
                ],
                answer="b",
                explanation="Compliance as code means translating regulatory requirements (SOC 2, HIPAA, PCI-DSS, CIS benchmarks) into automated checks — OPA policies, InSpec profiles, Terraform Sentinel rules — that run continuously. Instead of point-in-time audits with screenshots, you have real-time compliance verification. If a control drifts, you know immediately.",
                hints=["It's about automation, not documentation.", "Compliance checks run in the pipeline, not in a spreadsheet."],
                xp=25,
            ),
            Challenge(
                id="cac_cis_benchmarks",
                type="quiz",
                prompt="CIS Benchmarks are:",
                options=[
                    "Certification exams for security professionals",
                    "Consensus-based security configuration guidelines for operating systems, cloud platforms, databases, and containers",
                    "A type of encryption algorithm",
                    "A container image scanning tool",
                ],
                answer="b",
                explanation="CIS (Center for Internet Security) Benchmarks are detailed, prescriptive configuration guides: 'Ensure SSH root login is disabled,' 'Ensure CloudTrail is enabled in all regions,' 'Ensure Kubernetes API server audit logging is enabled.' Tools like kube-bench, InSpec, and Prowler automate CIS benchmark checks so you can verify compliance continuously.",
                hints=["CIS provides hardening guidelines for many platforms.", "They're configuration recommendations, not tools."],
                xp=25,
            ),
            Challenge(
                id="cac_opa_gatekeeper",
                type="quiz",
                prompt="Open Policy Agent (OPA) in a Kubernetes cluster (via Gatekeeper) enforces compliance by:",
                options=[
                    "Encrypting all pod-to-pod communication",
                    "Evaluating Rego policies against resource configurations at admission time, rejecting non-compliant resources",
                    "Automatically patching non-compliant resources to be compliant",
                    "Generating compliance reports for auditors",
                ],
                answer="b",
                explanation="OPA Gatekeeper is a Kubernetes admission controller that evaluates Rego policies. Policies can enforce: 'all containers must have resource limits,' 'no containers may run as root,' 'all images must come from approved registries.' Non-compliant resources are rejected at admission — compliance is enforced in real-time, not discovered after the fact.",
                hints=["It's an admission controller — it intercepts API requests.", "Rego is OPA's policy language."],
                xp=30,
            ),
            Challenge(
                id="cac_soc2_controls",
                type="quiz",
                prompt="SOC 2 Type II audits evaluate:",
                options=[
                    "The design of security controls at a single point in time",
                    "The operational effectiveness of security controls over a period of time (typically 6-12 months)",
                    "The number of security tools deployed",
                    "The security certifications held by staff",
                ],
                answer="b",
                explanation="SOC 2 Type I evaluates control design at a point in time. Type II evaluates operational effectiveness over a period — proving controls actually work consistently. This is why compliance-as-code is powerful: automated tests running on every commit provide continuous evidence that controls are operational, not just designed.",
                hints=["Type II is about operational effectiveness over time.", "The key difference from Type I is the time period."],
                xp=30,
            ),
            Challenge(
                id="cac_hipaa_safeguards",
                type="quiz",
                prompt="Under HIPAA, Protected Health Information (PHI) must have technical safeguards. Which of these is a required technical safeguard?",
                options=[
                    "Publishing a privacy policy on the company website",
                    "Implementing access controls, audit controls, integrity controls, and transmission security for electronic PHI",
                    "Having a Chief Medical Officer review all code changes",
                    "Storing PHI only in US-based data centers",
                ],
                answer="b",
                explanation="HIPAA's Technical Safeguards (164.312) require: access controls (unique user IDs, emergency access), audit controls (logs of PHI access), integrity controls (mechanisms to prevent unauthorized modification), and transmission security (encryption of PHI in transit). These map directly to automated checks: IAM policies, CloudTrail logging, encryption verification.",
                hints=["HIPAA has specific technical requirements for ePHI.", "Think: access, audit, integrity, transmission."],
                xp=30,
            ),
            Challenge(
                id="cac_inspec_purpose",
                type="text",
                prompt="Name the Chef-maintained compliance testing tool that lets you write 'compliance profiles' as code to verify infrastructure configuration against CIS benchmarks.",
                options=[],
                answer="InSpec",
                explanation="Chef InSpec lets you write compliance tests in a Ruby DSL: 'describe sshd_config do its(\"PermitRootLogin\") { should eq \"no\" } end'. Profiles can be shared, versioned, and run in CI/CD. Pre-built profiles exist for CIS benchmarks across Linux, Windows, AWS, Azure, GCP, and Kubernetes. Results feed directly into audit evidence.",
                hints=["It's from the Chef ecosystem.", "Profiles describe what the infrastructure should look like."],
                xp=30,
            ),
            Challenge(
                id="cac_prowler_aws",
                type="quiz",
                prompt="Prowler is an open-source tool that performs:",
                options=[
                    "Container orchestration across multiple clouds",
                    "AWS (and multi-cloud) security assessments against CIS, PCI-DSS, HIPAA, and other compliance frameworks",
                    "Automated penetration testing of web applications",
                    "Secret rotation for cloud provider credentials",
                ],
                answer="b",
                explanation="Prowler runs hundreds of checks against AWS (and now Azure/GCP) configurations: 'Is CloudTrail enabled?' 'Are S3 buckets public?' 'Is MFA on root?' Each check maps to specific compliance requirements (CIS AWS Benchmark, PCI-DSS, HIPAA). Running Prowler in CI ensures your cloud configuration stays compliant continuously.",
                hints=["It's specifically designed for cloud compliance checking.", "It maps findings to specific compliance frameworks."],
                xp=30,
            ),
            Challenge(
                id="cac_policy_as_code_drift",
                type="text",
                prompt="When infrastructure configuration drifts from its compliant state between deployments, what practice detects this drift automatically? (Two words.)",
                options=[],
                answer="continuous compliance",
                explanation="Continuous compliance means running compliance checks not just at deploy time, but on a schedule (e.g., hourly) against live infrastructure. Tools like Prowler, Cloud Custodian, and AWS Config Rules continuously evaluate the actual state of your infrastructure against policy. When drift is detected — someone manually changed a security group, disabled logging — an alert fires immediately.",
                hints=["It's the practice of checking compliance continuously, not periodically.", "The opposite of quarterly audits."],
                xp=35,
            ),
        ],
    ),
}
